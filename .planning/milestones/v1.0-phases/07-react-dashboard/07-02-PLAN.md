---
phase: 07-react-dashboard
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - dashboard/src/App.tsx
  - dashboard/src/components/ListingsTable.tsx
  - dashboard/src/components/Badge.tsx
  - dashboard/src/components/AgentDecisionPanel.tsx
  - dashboard/src/components/EscrowStatus.tsx
  - dashboard/src/components/WalletInspector.tsx
  - dashboard/src/components/ConfidenceBar.tsx
  - dashboard/src/hooks/useListings.ts
  - dashboard/src/hooks/useWallet.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "Dashboard shows a table of all scanned listings with classification badges"
    - "Badge colors match: SCALPING_VIOLATION=red, LIKELY_SCAM=orange, COUNTERFEIT_RISK=yellow, LEGITIMATE=green"
    - "Clicking a listing row expands an Agent Decision panel with reasoning and confidence"
    - "Wallet inspector shows address, USDT balance, and 'client-side only (WDK non-custodial)' badge"
    - "Escrow status section shows contract address and total locked USDT"
    - "All data auto-refreshes every 10 seconds"
    - "Dark theme with Ducket brand colors is applied throughout"
  artifacts:
    - path: "dashboard/src/components/ListingsTable.tsx"
      provides: "Main listings table with expandable rows"
      min_lines: 40
    - path: "dashboard/src/components/Badge.tsx"
      provides: "Classification badge component"
      contains: "SCALPING_VIOLATION"
    - path: "dashboard/src/components/AgentDecisionPanel.tsx"
      provides: "Expanded row detail panel"
      contains: "reasoning"
    - path: "dashboard/src/components/WalletInspector.tsx"
      provides: "Wallet address and non-custodial badge"
      contains: "WDK non-custodial"
    - path: "dashboard/src/hooks/useListings.ts"
      provides: "Polling hook for /api/listings"
      contains: "setInterval"
    - path: "dashboard/src/hooks/useWallet.ts"
      provides: "Polling hook for /api/wallet"
      contains: "setInterval"
  key_links:
    - from: "dashboard/src/hooks/useListings.ts"
      to: "/api/listings"
      via: "fetch + 10s polling"
      pattern: "fetch.*api/listings"
    - from: "dashboard/src/hooks/useWallet.ts"
      to: "/api/wallet"
      via: "fetch + 10s polling"
      pattern: "fetch.*api/wallet"
    - from: "dashboard/src/components/ListingsTable.tsx"
      to: "dashboard/src/components/AgentDecisionPanel.tsx"
      via: "row click expand state"
      pattern: "AgentDecisionPanel"
---

<objective>
Build all React UI components — listings table with expandable rows, classification badges, Agent Decision panels, escrow status, and wallet inspector — with 10-second polling hooks and Ducket brand styling.

Purpose: This is the visible product — the dashboard is the visual proof for judges of all agent work from phases 3-6.
Output: Complete dashboard UI that displays live agent data with proper styling and interactivity.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-react-dashboard/07-CONTEXT.md
@.planning/phases/07-react-dashboard/07-RESEARCH.md
@.planning/phases/07-react-dashboard/07-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 -->
From dashboard/src/types.ts:
```typescript
export interface Listing {
  platform: string;
  seller: string;
  price: number;
  faceValue: number;
  priceDeltaPct: number;
  url: string;
  listingDate: string;
  redFlags: string[];
  eventName: string;
  section: string | null;
  quantity: number;
  source: 'mock' | 'live';
  classification?: Classification;
}

export interface Classification {
  category: 'SCALPING_VIOLATION' | 'LIKELY_SCAM' | 'COUNTERFEIT_RISK' | 'LEGITIMATE';
  confidence: number;
  reasoning: string;
  classificationSource: string;
  actionTaken?: string;
  etherscanLink?: string;
}

export interface WalletInfo {
  address: string;
  ethBalance: string;
  usdtBalance: string;
  escrowContract: string;
  custodyType: string;
  network: string;
}
```

<!-- API endpoints from Plan 01 -->
- GET /api/listings → Listing[]
- GET /api/wallet → WalletInfo
- GET /api/cases/:urlHash → { filename, content }

<!-- Tailwind v4 brand tokens from Plan 01 index.css -->
- bg-bg-primary (#0A0E17), bg-bg-card (#1A1F2E)
- text-accent / bg-accent (#6366F1 indigo)
- bg-success (#10B981 green)
- bg-warn-red (#EF4444), bg-warn-orange (#F97316), bg-warn-yellow (#EAB308)
- font-sans = Inter, font-mono = monospace
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create polling hooks and reusable UI components (Badge, ConfidenceBar)</name>
  <read_first>
    - dashboard/src/types.ts
    - dashboard/src/index.css
    - dashboard/server/api.ts
    - .planning/phases/07-react-dashboard/07-RESEARCH.md
  </read_first>
  <files>
    dashboard/src/hooks/useListings.ts,
    dashboard/src/hooks/useWallet.ts,
    dashboard/src/components/Badge.tsx,
    dashboard/src/components/ConfidenceBar.tsx
  </files>
  <action>
    1. **dashboard/src/hooks/useListings.ts** — Polling hook:
       - `useListings()` returns `{ listings: Listing[], lastUpdated: Date | null, loading: boolean }`
       - Uses `useState` + `useEffect` + `setInterval(fetchListings, 10_000)`
       - Calls `window.fetch('/api/listings')` — parses JSON as `Listing[]`
       - Sets `loading: true` initially, `false` after first response
       - Cleans up interval on unmount via `clearInterval`
       - Import Listing type from `../types`

    2. **dashboard/src/hooks/useWallet.ts** — Polling hook:
       - `useWallet()` returns `{ wallet: WalletInfo | null, loading: boolean }`
       - Same pattern: `useState` + `useEffect` + `setInterval(fetchWallet, 10_000)`
       - Calls `window.fetch('/api/wallet')` — parses JSON as `WalletInfo`
       - Import WalletInfo type from `../types`

    3. **dashboard/src/components/Badge.tsx** — Classification badge:
       - Props: `{ category: string }`
       - Color map object:
         - `SCALPING_VIOLATION`: `bg-warn-red text-white`
         - `LIKELY_SCAM`: `bg-warn-orange text-white`
         - `COUNTERFEIT_RISK`: `bg-warn-yellow text-black`
         - `LEGITIMATE`: `bg-success text-white`
         - fallback: `bg-gray-500 text-white`
       - Renders `<span>` with `px-2 py-1 rounded text-xs font-semibold` + mapped color classes
       - Display text: category with underscores replaced by spaces

    4. **dashboard/src/components/ConfidenceBar.tsx** — Confidence visual:
       - Props: `{ value: number }`
       - Color logic: value >= 85 → `bg-warn-red`, value >= 60 → `bg-warn-orange`, else `bg-success`
       - Outer div: `w-full bg-bg-card rounded h-2`
       - Inner div: computed color class, `h-2 rounded`, `style={{ width: \`${value}%\` }}`
       - Show percentage text next to bar: `<span className="text-sm text-gray-400 ml-2">{value}%</span>`
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && npx vite build --config dashboard/vite.config.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/src/hooks/useListings.ts` contains `setInterval` with `10_000` and `fetch('/api/listings')`
    - `dashboard/src/hooks/useWallet.ts` contains `setInterval` with `10_000` and `fetch('/api/wallet')`
    - `dashboard/src/components/Badge.tsx` contains `SCALPING_VIOLATION`, `bg-warn-red`, `bg-warn-orange`, `bg-warn-yellow`, `bg-success`
    - `dashboard/src/components/ConfidenceBar.tsx` contains `value >= 85` threshold check
    - All files import from `../types`
    - Vite build succeeds
  </acceptance_criteria>
  <done>
    Polling hooks fetch data every 10 seconds. Badge renders color-coded classification labels. ConfidenceBar renders a percentage bar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ListingsTable, AgentDecisionPanel, EscrowStatus, WalletInspector, and wire App.tsx</name>
  <read_first>
    - dashboard/src/types.ts
    - dashboard/src/hooks/useListings.ts
    - dashboard/src/hooks/useWallet.ts
    - dashboard/src/components/Badge.tsx
    - dashboard/src/components/ConfidenceBar.tsx
    - dashboard/src/App.tsx
    - dashboard/src/index.css
  </read_first>
  <files>
    dashboard/src/components/ListingsTable.tsx,
    dashboard/src/components/AgentDecisionPanel.tsx,
    dashboard/src/components/EscrowStatus.tsx,
    dashboard/src/components/WalletInspector.tsx,
    dashboard/src/App.tsx
  </files>
  <action>
    1. **dashboard/src/components/AgentDecisionPanel.tsx**:
       - Props: `{ classification: Classification }` (import from `../types`)
       - Renders a `bg-bg-card/50 border border-accent/20 rounded-lg p-4` panel containing:
         - Row: "Category" label + Badge component
         - Row: "Confidence" label + ConfidenceBar component
         - Row: "Reasoning" label + reasoning text in `text-gray-300 text-sm`
         - Row: "Classification Source" label + classificationSource value
         - Conditional row: if `actionTaken` exists, show "Action Taken" label + value (green if release, red if escrow_deposit)
         - Conditional row: if `etherscanLink` exists, show clickable link in `text-accent underline font-mono text-xs` opening in new tab
       - Red flags list if present in listing

    2. **dashboard/src/components/ListingsTable.tsx**:
       - Props: `{ listings: Listing[] }`
       - Uses `useState<string | null>(null)` for `expandedUrl` (tracks which row is expanded)
       - Table columns per locked decision: Platform, Seller, Price, Face Value, Delta%, Classification, Confidence, Status
       - Table styling: `w-full` table, `bg-bg-card` header row, alternating row shading, `hover:bg-bg-card/70 cursor-pointer` on rows
       - Each row: click toggles `expandedUrl === listing.url`
       - Platform column: show source badge ("mock" in gray, "live" in green) after platform name
       - Price column: `$${listing.price.toLocaleString()}`
       - Face Value column: `$${listing.faceValue.toLocaleString()}`
       - Delta% column: `${listing.priceDeltaPct}%` — color red if > 100, orange if > 50, green otherwise
       - Classification column: `<Badge category={listing.classification?.category ?? 'PENDING'} />`
       - Confidence column: `<ConfidenceBar value={listing.classification?.confidence ?? 0} />`
       - Status column: `listing.classification?.actionTaken ?? 'pending'`
       - When `expandedUrl === listing.url` AND listing.classification exists: render `<AgentDecisionPanel>` in a full-width `<tr><td colSpan={8}>` below the listing row
       - Empty state: if listings.length === 0, show "Waiting for scan cycle..." in center with subtle pulse animation

    3. **dashboard/src/components/EscrowStatus.tsx**:
       - Props: `{ listings: Listing[], wallet: WalletInfo | null }`
       - Shows escrow contract address (monospace font) from wallet?.escrowContract
       - Calculates stats from listings: total listings, total with escrow_deposit action, total with release action
       - Displays summary cards in a grid: "Total Scanned" count, "Escrow Deposits" count, "Releases" count, "Active Escrows" (deposits - releases)
       - Card styling: `bg-bg-card rounded-lg p-4` with count in large `text-2xl font-bold text-accent` and label in `text-gray-400 text-sm`
       - Show network badge: "Sepolia Testnet" in `bg-accent/20 text-accent` pill

    4. **dashboard/src/components/WalletInspector.tsx**:
       - Props: `{ wallet: WalletInfo | null, loading: boolean }`
       - Loading state: show "Connecting to Sepolia..." with subtle pulse
       - Main layout: `bg-bg-card rounded-lg p-6` card
       - Row 1: "Wallet Address" label + address in `font-mono text-sm text-gray-300 break-all` + custody badge: `<span className="bg-accent text-white text-xs px-2 py-0.5 rounded-full ml-2">client-side only (WDK non-custodial)</span>`
       - Row 2: Two-column grid — "ETH Balance" card with value, "USDT Balance" card with value (both `font-mono text-lg`)
       - Row 3: "Escrow Contract" label + contract address in `font-mono text-xs text-gray-400`
       - Row 4: "Network" label + network value with green dot indicator
       - If wallet is null and not loading: show "Wallet unavailable — check SEPOLIA_RPC_URL" warning

    5. **dashboard/src/App.tsx** — Complete rewrite. Single page with tabbed sections per locked decision:
       - Import and use `useListings` and `useWallet` hooks
       - Three tabs: "Listings", "Escrow", "Wallet" — use `useState<string>('listings')` for active tab
       - Header: "Ducket AI Galactica" in `text-2xl font-bold text-white` with subtitle "Autonomous Fraud Detection Agent" in `text-gray-400`
       - Tab bar: horizontal buttons with `bg-accent text-white` for active, `bg-bg-card text-gray-400` for inactive, `rounded-t-lg px-6 py-2` styling
       - Below tab bar: last updated timestamp from useListings in `text-xs text-gray-500`
       - Tab content area:
         - "Listings" tab → `<ListingsTable listings={listings} />`
         - "Escrow" tab → `<EscrowStatus listings={listings} wallet={wallet} />`
         - "Wallet" tab → `<WalletInspector wallet={wallet} loading={walletLoading} />`
       - Outer container: `min-h-screen bg-bg-primary p-6 max-w-7xl mx-auto`
       - Footer: "Powered by WDK + Claude AI" in `text-gray-600 text-xs text-center mt-8`
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && npx vite build --config dashboard/vite.config.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/src/components/ListingsTable.tsx` contains columns: Platform, Seller, Price, Face Value, Delta%, Classification, Confidence, Status
    - `dashboard/src/components/ListingsTable.tsx` contains `expandedUrl` state and `AgentDecisionPanel` render
    - `dashboard/src/components/AgentDecisionPanel.tsx` contains `reasoning` and `etherscanLink` display
    - `dashboard/src/components/WalletInspector.tsx` contains the exact string `client-side only (WDK non-custodial)`
    - `dashboard/src/components/EscrowStatus.tsx` contains `escrow_deposit` count logic
    - `dashboard/src/App.tsx` contains tab state with `useState` and renders ListingsTable, EscrowStatus, WalletInspector
    - `dashboard/src/App.tsx` contains "Ducket AI Galactica" header text
    - Vite build succeeds with zero errors
  </acceptance_criteria>
  <done>
    Complete dashboard UI: listings table with expandable Agent Decision panels, escrow summary, wallet inspector with non-custodial badge. All tabs functional with 10-second auto-refresh. Ducket brand dark theme applied.
  </done>
</task>

</tasks>

<verification>
- `npx vite build --config dashboard/vite.config.ts` exits 0
- All component files exist in dashboard/src/components/
- Badge colors match specification
- WalletInspector contains "client-side only (WDK non-custodial)"
- App.tsx wires all components with tab navigation
</verification>

<success_criteria>
- Vite build succeeds
- Listings table renders with 8 specified columns
- Classification badges use correct colors per category
- Agent Decision panel shows reasoning, confidence, Etherscan link
- Wallet inspector displays "client-side only (WDK non-custodial)" badge
- 10-second auto-refresh via polling hooks
- Dark theme with Ducket brand colors applied
</success_criteria>

<output>
After completion, create `.planning/phases/07-react-dashboard/07-02-SUMMARY.md`
</output>
