---
phase: 07-react-dashboard
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "Dashboard visually shows listings table with color-coded badges"
    - "Clicking a row expands Agent Decision panel with reasoning"
    - "Wallet inspector shows address and non-custodial badge"
    - "Dark theme with Ducket brand colors renders correctly"
    - "Data refreshes within 10 seconds of agent writing new data"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete dashboard — all five ROADMAP success criteria confirmed by human inspection in a running browser.

Purpose: Dashboard is a visual product. Build checks confirm code compiles but cannot verify colors, layout, interactivity, or data display. Human eyes are required.
Output: Confirmed dashboard ready for Phase 8 demo integration.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-react-dashboard/07-CONTEXT.md
@.planning/phases/07-react-dashboard/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of complete dashboard</name>
  <files>dashboard/src/App.tsx</files>
  <read_first>
    - .planning/phases/07-react-dashboard/07-02-SUMMARY.md
  </read_first>
  <action>
    Start the dashboard dev server and present it to the user for visual verification.

    1. Start the agent scan loop in the background (if not running) to produce data:
       `cd agent && node src/scan-loop.js &`
    2. Wait ~30 seconds for first scan cycle to populate LISTINGS.md and cases/
    3. Start the dashboard: `cd dashboard && npm run dev`
    4. Open http://localhost:5173 in browser
    5. Present each of the 5 verification checks below to the user for confirmation
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && npx vite build --config dashboard/vite.config.ts 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms all 5 ROADMAP success criteria pass visual inspection.</done>
  <what-built>
    Complete React dashboard with:
    - Listings table with classification badges (red/orange/yellow/green)
    - Expandable Agent Decision panels (reasoning, confidence bar, Etherscan link)
    - Escrow status summary (deposit count, release count, active escrows)
    - Wallet inspector (address, balances, "client-side only (WDK non-custodial)" badge)
    - Dark theme with Ducket brand colors (#0A0E17 background, #6366F1 indigo accent)
    - 10-second auto-refresh polling
  </what-built>
  <how-to-verify>
    **Pre-flight:** Start the agent first so data exists:
    ```
    cd agent && node src/scan-loop.js &
    ```
    Wait ~30 seconds for first scan cycle, then:
    ```
    cd dashboard && npm run dev
    ```
    Open http://localhost:5173 in browser.

    **Check each success criterion:**

    1. **DASH-01 — Listings table with badges:**
       - Verify table shows Platform, Seller, Price, Face Value, Delta%, Classification, Confidence, Status columns
       - Verify SCALPING_VIOLATION badges are RED
       - Verify LIKELY_SCAM badges are ORANGE
       - Verify COUNTERFEIT_RISK badges are YELLOW
       - Verify LEGITIMATE badges are GREEN
       - Click a listing row — Agent Decision panel should expand below it showing reasoning text and confidence bar

    2. **DASH-02 — Escrow status:**
       - Click "Escrow" tab
       - Verify escrow contract address is displayed
       - Verify deposit/release/active escrow counts are shown
       - Verify "Sepolia Testnet" network badge is present

    3. **DASH-03 — Wallet inspector:**
       - Click "Wallet" tab
       - Verify wallet address is displayed in monospace font
       - Verify "client-side only (WDK non-custodial)" badge is visible
       - Verify ETH and USDT balances are shown (may be 0 if no Sepolia RPC configured)

    4. **DASH-04 — Ducket brand styling:**
       - Verify dark background (#0A0E17)
       - Verify card backgrounds are slightly lighter (#1A1F2E)
       - Verify indigo accent color (#6366F1) on active tab and header
       - Verify Inter font is loaded (check via DevTools > Computed styles)

    5. **Auto-refresh:**
       - Leave dashboard open for 20 seconds
       - Verify "Last updated" timestamp changes (should update every ~10 seconds)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe specific issues to fix</resume-signal>
  <acceptance_criteria>
    - Human confirms all 5 ROADMAP success criteria are visually verified
    - Classification badge colors are correct
    - Agent Decision panel expands on row click
    - Wallet inspector shows "client-side only (WDK non-custodial)"
    - Dark theme renders with correct brand colors
  </acceptance_criteria>
</task>

</tasks>

<verification>
All 5 Phase 7 ROADMAP success criteria confirmed by human visual inspection.
</verification>

<success_criteria>
Human approves dashboard appearance, interactivity, data display, and brand styling.
</success_criteria>

<output>
After completion, create `.planning/phases/07-react-dashboard/07-03-SUMMARY.md`
</output>
