---
phase: 08-demo-integration-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - .env.example
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04

must_haves:
  truths:
    - "Running `npm run demo` from root starts both agent scan loop and dashboard concurrently"
    - "README setup instructions (clone, install, cp .env, npm run demo) lead to a running system"
    - ".env.example contains every env var needed with explaining comments"
    - "README includes architecture diagram, demo video placeholder, third-party disclosures, Apache 2.0 badge"
  artifacts:
    - path: "package.json"
      provides: "demo script using concurrently"
      contains: "demo"
    - path: "README.md"
      provides: "Complete submission README"
      contains: "Architecture"
    - path: ".env.example"
      provides: "All env vars with comments"
      contains: "BOUNTY_POOL_ADDRESS"
  key_links:
    - from: "package.json"
      to: "agent/src/scan-loop.js"
      via: "npm run demo launches scan-loop"
      pattern: "scan-loop"
    - from: "package.json"
      to: "dashboard/package.json"
      via: "npm run demo launches dashboard dev"
      pattern: "dev:dashboard"
---

<objective>
Create the `npm run demo` script, rewrite README.md for hackathon submission, and update .env.example with all env vars.

Purpose: A judge cloning the repo can follow README instructions to reach a running agent + dashboard with a single command. All submission checklist items pass.
Output: Root package.json with demo script, submission-ready README.md, complete .env.example
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-integration-submission/08-CONTEXT.md
@package.json
@README.md
@.env.example
@agent/package.json
@dashboard/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add demo script to root package.json and update .env.example</name>
  <files>package.json, .env.example</files>
  <read_first>package.json, .env.example, agent/package.json, dashboard/package.json, agent/src/scan-loop.js</read_first>
  <action>
1. In root `package.json`, add a `"demo"` script that uses concurrently to run both the agent scan loop and the dashboard in parallel. The command should be:
   ```
   "demo": "concurrently --names agent,dashboard --prefix-colors blue,green \"node agent/src/scan-loop.js\" \"npm run dev --workspace=dashboard\""
   ```
   Also add `concurrently` to root devDependencies (version `^9.2.1` — same as dashboard uses) so `npm run demo` works from root without relying on workspace hoisting.

2. Update `.env.example` to include ALL env vars used across the project with explaining comments. The complete list:
   ```
   # === Anthropic Claude API ===
   # Required for AI-powered fraud classification
   CLAUDE_API_KEY=your_claude_api_key_here
   CLAUDE_MODEL=claude-sonnet-4-6

   # === WDK Wallet (Tether) ===
   # Self-custodial USDT wallet — 12-word BIP-39 seed phrase
   ESCROW_WALLET_SEED=twelve word seed phrase here never commit real seed
   WDK_API_KEY=your_wdk_api_key_here

   # === Sepolia Network ===
   # Ethereum testnet for escrow contract and USDT
   SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/your_project_id
   SEPOLIA_USDT_CONTRACT=0x7169d38820dfd117c3fa1f22a697dba58d90ba06
   SEPOLIA_DEPLOYER_PRIVATE_KEY=0xyour_deployer_private_key_here

   # === Escrow Contract ===
   # Address of the deployed FraudEscrow.sol on Sepolia (from contracts/deployed.json)
   BOUNTY_POOL_ADDRESS=

   # === Agent Config ===
   # How often the scan loop polls marketplaces (minutes)
   SCAN_INTERVAL_MINUTES=5
   # Minimum classification confidence to trigger escrow action (0-100)
   FRAUD_CONFIDENCE_THRESHOLD=85
   # Target event name for scraping
   EVENT_NAME=FIFA World Cup 2026

   # === Scraper Config (optional) ===
   # Navigation timeout for StubHub scraper (ms, default: 60000)
   STUBHUB_TIMEOUT=60000
   ```
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && node -e "const p=require('./package.json'); if(!p.scripts.demo) process.exit(1); if(!p.scripts.demo.includes('concurrently')) process.exit(1); console.log('demo script OK:', p.scripts.demo)" && grep -q "BOUNTY_POOL_ADDRESS" .env.example && grep -q "EVENT_NAME" .env.example && grep -q "STUBHUB_TIMEOUT" .env.example && echo ".env.example OK"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"demo"` in scripts object with `concurrently` command
    - `package.json` contains `"concurrently"` in devDependencies
    - `.env.example` contains all 12 env vars: CLAUDE_API_KEY, CLAUDE_MODEL, ESCROW_WALLET_SEED, WDK_API_KEY, SEPOLIA_RPC_URL, SEPOLIA_USDT_CONTRACT, SEPOLIA_DEPLOYER_PRIVATE_KEY, BOUNTY_POOL_ADDRESS, SCAN_INTERVAL_MINUTES, FRAUD_CONFIDENCE_THRESHOLD, EVENT_NAME, STUBHUB_TIMEOUT
    - Each env var in .env.example has an explaining comment above it
  </acceptance_criteria>
  <done>Root `npm run demo` script launches agent + dashboard concurrently. .env.example has all vars with comments.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md for hackathon submission</name>
  <files>README.md</files>
  <read_first>README.md, .env.example, package.json, agent/package.json, dashboard/package.json, CLAUDE.md</read_first>
  <action>
Rewrite README.md with the following structure. Keep it concise and judge-friendly.

**Section 1: Header + Badge**
```
# Ducket AI Galactica

[![License: Apache 2.0](https://img.shields.io/badge/License-Apache_2.0-blue.svg)](./LICENSE)
```

**Section 2: One-liner**
"Autonomous fraud detection agent that scans secondary ticket markets, classifies fraud with AI, and enforces outcomes via USDT escrow on Sepolia — no human in the loop."

**Section 3: Architecture Diagram (text)**
```
                         Ducket AI Galactica
                    ============================

    +-----------+     +-----------+     +------------+
    | StubHub   |     | Viagogo   |     | Facebook   |
    | Scraper   |     | Scraper   |     | Marketplace|
    +-----+-----+     +-----+-----+     +------+-----+
          |                 |                   |
          +--------+--------+--------+----------+
                   |                 |
                   v                 v
          +--------+--------+  +----+-----+
          | Scan Loop       |  | Evidence  |
          | (node-cron 5m)  |  | Case Files|
          +--------+--------+  +----------+
                   |
                   v
          +--------+--------+
          | Classification  |
          | (Rules + Claude)|
          +--------+--------+
                   |
                   v
          +--------+--------+
          | Escrow Engine   |
          | (WDK + Sepolia) |
          +--------+--------+
                   |
                   v
          +--------+--------+
          | FraudEscrow.sol |
          | (USDT on-chain) |
          +-----------------+

    +---------------------------+
    | React Dashboard (Vite)    |
    | Listings + Classifications|
    | Escrow Status + Wallet    |
    +---------------------------+
```

**Section 4: Quick Start**
```
## Quick Start

### Prerequisites
- Node.js >= 20.0.0
- npm >= 9.0.0

### Setup
git clone https://github.com/niccoloducket/ducket-ai-galactica.git
cd ducket-ai-galactica
npm install
cp .env.example .env
# Fill in your API keys (see .env.example for details)

### Run
npm run demo
# Starts agent scan loop + React dashboard simultaneously
# Agent: polls StubHub/Viagogo/Facebook every 5 minutes
# Dashboard: http://localhost:5173
```

**Section 5: How It Works**
Brief description of the 4 demo segments:
1. **Agent Intelligence** — Autonomous scan loop polls 3 platforms, no human trigger
2. **Wallet Flow** — WDK non-custodial USDT wallet on Sepolia, keys never leave client
3. **Payment Lifecycle** — Escrow deposit/release/refund/slash based on classification
4. **Live Full Loop** — Event input -> scan -> classify -> escrow action in under 5 minutes

**Section 6: Project Structure**
```
ducket-ai-galactica/
  agent/             Autonomous fraud detection agent
    src/             Core pipeline (scan-loop, classify, escrow, evidence)
    tools/           Platform scrapers (StubHub, Viagogo, Facebook)
    memory/          Scan results (LISTINGS.md)
    cases/           Evidence case files per listing
  dashboard/         React dashboard (Vite + Tailwind v4)
    src/             UI components
    server/          Express API serving agent data
  contracts/         FraudEscrow.sol (Hardhat 3)
```

**Section 7: Demo Video**
```
## Demo Video

[VIDEO LINK — to be added before submission]
```

**Section 8: Third-Party Disclosures**
Table format with all services:
| Service | Provider | Purpose |
|---------|----------|---------|
| Claude API | Anthropic | AI fraud classification |
| WDK | Tether | Non-custodial USDT wallet |
| Patchright | playwright fork | Anti-bot scraping |
| ethers.js | ethers | Blockchain interaction |
| node-cron | node-cron | Scan loop scheduling |
| Vite | Vite | Dashboard build tool |
| React | Meta | Dashboard UI |
| Tailwind CSS | Tailwind Labs | Dashboard styling |
| Sepolia Testnet | Ethereum Foundation | Test blockchain |

**Section 9: Environment Variables**
Point to .env.example:
"See `.env.example` for all required variables with explaining comments."

**Section 10: License**
"Apache 2.0 — see [LICENSE](./LICENSE)"
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && grep -q "Apache_2.0" README.md && grep -q "Architecture" README.md && grep -q "VIDEO LINK" README.md && grep -q "npm run demo" README.md && grep -q "Third-Party" README.md && grep -q "Quick Start" README.md && grep -q "How It Works" README.md && echo "README sections OK"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains Apache 2.0 badge image link
    - README.md contains text architecture diagram with "Scan Loop", "Classification", "Escrow Engine", "FraudEscrow.sol", "React Dashboard"
    - README.md contains Quick Start section with `npm run demo` as the primary run command
    - README.md contains "How It Works" with 4 segments: Agent Intelligence, Wallet Flow, Payment Lifecycle, Live Full Loop
    - README.md contains "Demo Video" section with "[VIDEO LINK"
    - README.md contains "Third-Party" disclosures table with Claude API, WDK, Patchright, ethers.js, node-cron, Vite, React, Tailwind CSS, Sepolia Testnet
    - README.md contains "Project Structure" section
    - README.md does NOT contain old "Run the agent" / "Run the dashboard" as separate commands (replaced by single npm run demo)
  </acceptance_criteria>
  <done>README.md is submission-ready with architecture diagram, single demo command, video placeholder, and full third-party disclosures.</done>
</task>

</tasks>

<verification>
- `npm run demo` script exists and references concurrently
- `.env.example` has all 12 env vars with comments
- README.md has all required sections for hackathon submission
- `grep -c "npm run demo" README.md` returns at least 2 (Quick Start + reference)
</verification>

<success_criteria>
- A new developer can follow README.md to reach a running system: clone, npm install, cp .env, npm run demo
- All submission documentation is complete (architecture, disclosures, video placeholder, license badge)
- .env.example is a complete reference for all required configuration
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-integration-submission/08-01-SUMMARY.md`
</output>
