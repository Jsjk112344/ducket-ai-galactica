---
phase: 01-project-scaffold-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - LICENSE
  - .env.example
  - package.json
  - tsconfig.base.json
  - agent/package.json
  - agent/tsconfig.json
  - agent/src/index.ts
  - dashboard/package.json
  - dashboard/tsconfig.json
  - dashboard/src/index.tsx
  - contracts/package.json
  - contracts/tsconfig.json
  - contracts/src/FraudEscrow.sol
  - README.md
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "LICENSE file at repo root contains the full Apache 2.0 license text"
    - ".gitignore blocks .env, *.key, node_modules, wallet.json, and all secret patterns"
    - "README.md lists all 5 third-party services (Claude API, WDK, Patchright, OpenClaw, Sepolia) in a disclosure table"
    - "npm install from repo root succeeds without errors across all 3 workspaces"
    - ".env.example exists with placeholder values for all known env vars and is not gitignored"
  artifacts:
    - path: "LICENSE"
      provides: "Apache 2.0 full license text"
      contains: "Apache License, Version 2.0"
    - path: ".gitignore"
      provides: "Secrets and build artifact exclusion"
      contains: ".env"
    - path: "README.md"
      provides: "Project description, setup instructions, third-party disclosure"
      contains: "Third-Party Services"
    - path: ".env.example"
      provides: "Environment variable template for judges"
      contains: "CLAUDE_API_KEY"
    - path: "package.json"
      provides: "npm workspaces root config"
      contains: "workspaces"
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript compiler options"
      contains: "strict"
    - path: "agent/package.json"
      provides: "Agent workspace package"
      contains: "@ducket/agent"
    - path: "dashboard/package.json"
      provides: "Dashboard workspace package"
      contains: "@ducket/dashboard"
    - path: "contracts/package.json"
      provides: "Contracts workspace package"
      contains: "@ducket/contracts"
  key_links:
    - from: "package.json"
      to: "agent/, dashboard/, contracts/"
      via: "workspaces array"
      pattern: '"workspaces".*agent.*dashboard.*contracts'
    - from: "agent/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends field"
      pattern: '"extends".*tsconfig.base.json'
---

<objective>
Create the complete project scaffold: LICENSE, .gitignore, README with third-party disclosures, .env.example, npm workspaces monorepo structure with 3 workspace directories (agent, dashboard, contracts), shared TypeScript config, and placeholder source files.

Purpose: Make the repo submittable from day one -- a judge can clone it, run npm install, and find no disqualifying compliance issues before any feature code is written.
Output: All compliance artifacts (COMP-01 through COMP-04) satisfied. Monorepo structure ready for Phase 2+.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-compliance/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compliance files and monorepo scaffold</name>
  <files>.gitignore, LICENSE, .env.example, package.json, tsconfig.base.json, agent/package.json, agent/tsconfig.json, agent/src/index.ts, dashboard/package.json, dashboard/tsconfig.json, dashboard/src/index.tsx, contracts/package.json, contracts/tsconfig.json, contracts/src/FraudEscrow.sol</files>
  <read_first>
    - .gitignore (current file -- only contains ".planning/", must be replaced with comprehensive version)
    - .planning/phases/01-project-scaffold-compliance/01-RESEARCH.md (contains exact file contents to use)
    - CLAUDE.md (project constraints)
  </read_first>
  <action>
Create all files in this exact order (gitignore FIRST to prevent accidental secret commits):

1. **Overwrite `.gitignore`** with comprehensive patterns. MUST include these exact lines:
```
# Node.js
node_modules/
dist/
build/
*.tsbuildinfo

# Secrets — NEVER commit these
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*.secret
*_secret*
*_private*
*credentials*
*.keystore
wallet.json
keystore/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/

# Logs
*.log
npm-debug.log*

# Test artifacts
coverage/
.nyc_output/

# Build artifacts
*.js.map
```
NOTE: Do NOT include `.planning/` in the new .gitignore -- .planning/ is tracked in git.

2. **Create `LICENSE`** with the full Apache License, Version 2.0 text. Must start with:
```
                                 Apache License
                           Version 2.0, January 2004
                        http://www.apache.org/licenses/
```
Use the complete official text from https://www.apache.org/licenses/LICENSE-2.0.txt verbatim. Do NOT use a summary or abbreviated version.

3. **Create `.env.example`** with these exact placeholder values:
```bash
# Ducket AI Galactica — Environment Variables
# Copy to .env and fill in real values (never commit .env)

# Anthropic Claude API
CLAUDE_API_KEY=your_claude_api_key_here
CLAUDE_MODEL=claude-opus-4-5

# WDK Wallet
ESCROW_WALLET_SEED=twelve word seed phrase here never commit real seed
WDK_API_KEY=your_wdk_api_key_here

# Sepolia Network
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/your_project_id
SEPOLIA_USDT_CONTRACT=0x7169d38820dfd117c3fa1f22a697dba58d90ba06

# Agent Config
SCAN_INTERVAL_MINUTES=5
FRAUD_CONFIDENCE_THRESHOLD=85
```

4. **Create root `package.json`**:
```json
{
  "name": "ducket-ai-galactica",
  "version": "1.0.0",
  "private": true,
  "description": "Autonomous fraud detection agent with USDT escrow enforcement",
  "workspaces": ["agent", "dashboard", "contracts"],
  "scripts": {
    "dev:agent": "npm run dev --workspace=agent",
    "dev:dashboard": "npm run dev --workspace=dashboard",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/niccoloducket/ducket-ai-galactica"
  }
}
```

5. **Create `tsconfig.base.json`**:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "outDir": "dist",
    "declaration": true
  },
  "exclude": ["node_modules", "dist"]
}
```

6. **Create `agent/package.json`**:
```json
{
  "name": "@ducket/agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0"
  }
}
```

7. **Create `agent/tsconfig.json`**:
```json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
```

8. **Create `agent/src/index.ts`**:
```typescript
// Ducket AI Galactica — Agent Entry Point
// Autonomous fraud detection agent with USDT escrow enforcement
// Implementation begins in Phase 2 (WDK Wallet) and Phase 3 (Scraping)

console.log("Ducket Agent — not yet implemented. See ROADMAP Phase 2+.");
```

9. **Create `dashboard/package.json`**:
```json
{
  "name": "@ducket/dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Dashboard not yet configured — see Phase 7'",
    "build": "echo 'Dashboard not yet configured — see Phase 7'"
  },
  "dependencies": {},
  "devDependencies": {}
}
```

10. **Create `dashboard/tsconfig.json`**:
```json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
```

11. **Create `dashboard/src/index.tsx`**:
```typescript
// Ducket AI Galactica — Dashboard Entry Point
// React dashboard for live agent monitoring
// Implementation begins in Phase 7

export {};
console.log("Ducket Dashboard — not yet implemented. See ROADMAP Phase 7.");
```

12. **Create `contracts/package.json`**:
```json
{
  "name": "@ducket/contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "echo 'Contracts not yet configured — see Phase 2'",
    "deploy": "echo 'Contracts not yet configured — see Phase 2'"
  },
  "dependencies": {},
  "devDependencies": {}
}
```

13. **Create `contracts/tsconfig.json`**:
```json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
```

14. **Create `contracts/src/FraudEscrow.sol`**:
```solidity
// SPDX-License-Identifier: Apache-2.0
// Ducket AI Galactica — Fraud Escrow Contract
// Implementation begins in Phase 2
pragma solidity ^0.8.20;

// Placeholder — FraudEscrow contract will be implemented in Phase 2
```
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && cat LICENSE | grep -q "Apache License, Version 2.0" && echo "LICENSE OK" && cat .gitignore | grep -q "^\.env$" && echo "GITIGNORE OK" && cat .env.example | grep -q "CLAUDE_API_KEY" && echo "ENV_EXAMPLE OK" && cat package.json | grep -q '"workspaces"' && echo "ROOT_PKG OK" && cat tsconfig.base.json | grep -q '"strict": true' && echo "TSCONFIG OK" && cat agent/package.json | grep -q "@ducket/agent" && echo "AGENT_PKG OK" && cat dashboard/package.json | grep -q "@ducket/dashboard" && echo "DASH_PKG OK" && cat contracts/package.json | grep -q "@ducket/contracts" && echo "CONTRACTS_PKG OK" && test -f agent/src/index.ts && echo "AGENT_SRC OK" && test -f dashboard/src/index.tsx && echo "DASH_SRC OK" && test -f contracts/src/FraudEscrow.sol && echo "SOL_SRC OK" && npm install --dry-run 2>&1 | tail -3 && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - LICENSE contains "Apache License, Version 2.0, January 2004"
    - .gitignore contains ".env" on its own line (not just .env.example)
    - .gitignore contains "!.env.example" (whitelist)
    - .gitignore contains "*.key", "wallet.json", "node_modules/"
    - .gitignore does NOT contain ".planning/"
    - .env.example contains "CLAUDE_API_KEY=your_claude_api_key_here"
    - .env.example contains "ESCROW_WALLET_SEED=twelve word seed phrase here never commit real seed"
    - .env.example contains "SEPOLIA_RPC_URL="
    - package.json contains '"workspaces": ["agent", "dashboard", "contracts"]'
    - package.json contains '"private": true'
    - package.json contains '"license": "Apache-2.0"'
    - tsconfig.base.json contains '"strict": true'
    - tsconfig.base.json contains '"module": "NodeNext"'
    - agent/package.json contains '"name": "@ducket/agent"'
    - agent/package.json contains '"private": true'
    - dashboard/package.json contains '"name": "@ducket/dashboard"'
    - dashboard/tsconfig.json contains '"jsx": "react-jsx"'
    - contracts/package.json contains '"name": "@ducket/contracts"'
    - contracts/src/FraudEscrow.sol contains "SPDX-License-Identifier: Apache-2.0"
    - agent/src/index.ts exists and is non-empty
    - dashboard/src/index.tsx exists and is non-empty
    - `npm install --dry-run` exits 0 from repo root
  </acceptance_criteria>
  <done>All 14 files created. LICENSE has full Apache 2.0 text. .gitignore blocks all secret patterns. .env.example has all known env vars with placeholders. npm workspaces root links agent, dashboard, and contracts. Each workspace has package.json (private:true, @ducket/ scoped), tsconfig.json extending base, and placeholder source file. npm install --dry-run succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with setup instructions and third-party disclosure</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-project-scaffold-compliance/01-RESEARCH.md (contains disclosure table content)
    - .planning/PROJECT.md (project description)
    - CLAUDE.md (hard constraints)
    - .env.example (to reference in setup instructions)
  </read_first>
  <action>
Create `README.md` at repo root with the following exact structure and content:

```markdown
# Ducket AI Galactica

Autonomous fraud detection agent with USDT escrow enforcement for the Tether Hackathon Galactica: WDK Edition 1.

## What It Does

An autonomous agent that scans secondary ticketing marketplaces (StubHub, Viagogo, Facebook Marketplace) for fraudulent or overpriced ticket listings, classifies each listing (scalping, scam, counterfeit, legitimate), and enforces outcomes on-chain via USDT escrow — all without human intervention.

**Core loop:** Scan -> Classify -> Act (no human triggers)

## Quick Start

### Prerequisites

- Node.js >= 20.0.0
- npm >= 9.0.0

### Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/niccoloducket/ducket-ai-galactica.git
   cd ducket-ai-galactica
   ```

2. Install dependencies:
   ```bash
   npm install
   ```

3. Configure environment variables:
   ```bash
   cp .env.example .env
   # Edit .env with your actual values (see .env.example for required variables)
   ```

4. Run the agent:
   ```bash
   npm run dev:agent
   ```

5. Run the dashboard:
   ```bash
   npm run dev:dashboard
   ```

## Project Structure

```
ducket-ai-galactica/
├── agent/          # Autonomous fraud detection agent
├── dashboard/      # React dashboard for live monitoring
├── contracts/      # FraudEscrow.sol smart contract
├── .env.example    # Required environment variables (copy to .env)
├── LICENSE         # Apache 2.0
└── README.md       # This file
```

## Third-Party Services Disclosure

This project uses the following external services. See individual service terms for usage restrictions.

| Service | Provider | Purpose | Required? |
|---------|----------|---------|-----------|
| Claude API | Anthropic | AI-powered fraud classification engine | Yes |
| WDK (Wallet Development Kit) | TinyFish / Tetherto | Non-custodial USDT wallet on Sepolia | Yes |
| Patchright | microsoft/playwright fork | Anti-bot browser automation for scraping | Yes |
| OpenClaw | OpenClaw | Autonomous agent heartbeat loop orchestration | Yes |
| Sepolia Testnet | Ethereum Foundation | Testnet blockchain for USDT escrow (no real funds) | Yes |

All third-party services are used in accordance with their respective terms of service.
No mainnet funds are used — Sepolia testnet only.

## Environment Variables

See `.env.example` for the full list of required environment variables. Key variables:

| Variable | Purpose |
|----------|---------|
| `CLAUDE_API_KEY` | Anthropic Claude API key for fraud classification |
| `ESCROW_WALLET_SEED` | WDK wallet seed phrase (never commit real values) |
| `WDK_API_KEY` | WDK API key |
| `SEPOLIA_RPC_URL` | Sepolia Ethereum RPC endpoint |
| `SEPOLIA_USDT_CONTRACT` | USDT contract address on Sepolia testnet |
| `SCAN_INTERVAL_MINUTES` | How often the agent polls marketplaces (default: 5) |
| `FRAUD_CONFIDENCE_THRESHOLD` | Minimum confidence to trigger escrow action (default: 85) |

## License

Apache 2.0 — see [LICENSE](./LICENSE) for full text.
```

The README must contain:
- The heading "## Third-Party Services Disclosure" (exact text, for grep verification)
- All 5 services: "Claude API", "WDK", "Patchright", "OpenClaw", "Sepolia Testnet"
- The heading "## Quick Start" with clone/install/run instructions
- The heading "## Environment Variables" referencing .env.example
- The heading "## License" pointing to LICENSE file
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && grep -q "Third-Party Services Disclosure" README.md && echo "DISCLOSURE_HEADING OK" && grep -q "Claude API" README.md && echo "CLAUDE_DISCLOSED OK" && grep -q "WDK" README.md && echo "WDK_DISCLOSED OK" && grep -q "Patchright" README.md && echo "PATCHRIGHT_DISCLOSED OK" && grep -q "OpenClaw" README.md && echo "OPENCLAW_DISCLOSED OK" && grep -q "Sepolia Testnet" README.md && echo "SEPOLIA_DISCLOSED OK" && grep -q "Quick Start" README.md && echo "QUICKSTART OK" && grep -q "Apache 2.0" README.md && echo "LICENSE_REF OK" && echo "ALL README CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "# Ducket AI Galactica" as first line
    - README.md contains "## Third-Party Services Disclosure" heading
    - README.md contains "Claude API" in the disclosure table
    - README.md contains "WDK" in the disclosure table
    - README.md contains "Patchright" in the disclosure table
    - README.md contains "OpenClaw" in the disclosure table
    - README.md contains "Sepolia Testnet" in the disclosure table
    - README.md contains "## Quick Start" heading
    - README.md contains "npm install" in setup instructions
    - README.md contains "## Environment Variables" heading
    - README.md contains "## License" heading with "Apache 2.0"
    - README.md contains "git clone" command
  </acceptance_criteria>
  <done>README.md at repo root with project description, Quick Start (clone, install, run), project structure overview, Third-Party Services Disclosure table listing all 5 services, Environment Variables table, and License section. Judges can read the README and understand what the project is, how to run it, and what external services it depends on.</done>
</task>

</tasks>

<verification>
Run all compliance checks in sequence:

1. `cat LICENSE | grep -q "Apache License, Version 2.0" && echo "COMP-01 PASS"`
2. `grep -q "Third-Party Services Disclosure" README.md && grep -q "Claude API" README.md && grep -q "WDK" README.md && grep -q "Patchright" README.md && grep -q "OpenClaw" README.md && grep -q "Sepolia" README.md && echo "COMP-02 PASS"`
3. `git status --short | grep -E "\.env[^.]|\.key$|secret" | grep -v ".env.example" | grep -v ".gitignore" && echo "COMP-03 FAIL" || echo "COMP-03 PASS"`
4. `npm install --dry-run && echo "COMP-04 PASS"`
</verification>

<success_criteria>
- COMP-01: LICENSE file at repo root contains full Apache 2.0 text
- COMP-02: README.md has Third-Party Services Disclosure section listing all 5 services
- COMP-03: .gitignore blocks .env, *.key, wallet.json, node_modules; no secrets in git status
- COMP-04: npm install succeeds from root; all 3 workspaces resolve; .env.example exists for judge setup
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-compliance/01-01-SUMMARY.md`
</output>
