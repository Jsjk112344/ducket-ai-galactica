---
phase: 09-reframe-narrative
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - docs/DEMO-SCRIPT.md
autonomous: true
requirements:
  - NARR-01
  - NARR-02
  - NARR-03

must_haves:
  truths:
    - "Every sentence in README describes a P2P resale platform, not a fraud monitoring tool"
    - "CLAUDE.md decision rules drive development through a buyer/seller lens with concrete directives"
    - "Demo script walks Alice (seller) and Bob (buyer) through 4 resale steps mapped to dashboard screens"
    - "Zero occurrences of banned phrases (fraud detection, monitoring tool, scan and report) in any target file"
  artifacts:
    - path: "README.md"
      provides: "P2P resale narrative for judges"
      contains: "Safe P2P ticket resale"
    - path: "CLAUDE.md"
      provides: "Development instructions with resale context"
      contains: "Safe P2P ticket resale platform"
    - path: "docs/DEMO-SCRIPT.md"
      provides: "Step-by-step demo walkthrough with Alice/Bob personas"
      contains: "Alice"
  key_links:
    - from: "README.md"
      to: "CLAUDE.md"
      via: "Consistent resale terminology"
      pattern: "Safe P2P ticket resale"
    - from: "docs/DEMO-SCRIPT.md"
      to: "README.md"
      via: "Same 4-step flow described in both"
      pattern: "seller lists.*buyer locks.*AI verif.*settles"
---

<objective>
Rewrite all project text so judges read a safe P2P ticket resale platform, not a fraud monitoring tool. Three files: README.md (full rewrite), CLAUDE.md (partial rewrite), and docs/DEMO-SCRIPT.md (new file).

Purpose: The v2.0 pivot reframes the same underlying tech (scan loop, classification engine, escrow) as a buyer/seller resale platform. This phase ensures every word judges read supports that narrative.
Output: Three updated/created text files with zero banned-phrase occurrences.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-reframe-narrative/09-CONTEXT.md
@.planning/phases/09-reframe-narrative/09-RESEARCH.md

<interfaces>
<!-- No code interfaces needed -- this is a pure text-rewrite phase -->
<!-- Key reference: Terminology Substitution Map from 09-RESEARCH.md -->

Banned phrases (grep targets -- zero matches required):
- "fraud detection"
- "monitoring tool"
- "scan and report"
- "suspicious" (in user-facing context)

Required phrases (must appear):
- "Safe P2P ticket resale"
- "seller lists" / "buyer locks USDT" / "AI verifies" / "escrow settles"
- "Alice" and "Bob" (demo script only)

Canonical 4-step flow:
1. Seller lists ticket
2. Buyer locks USDT in escrow
3. AI agent verifies ticket legitimacy
4. Escrow settles (releases to seller / refunds buyer)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md and CLAUDE.md with P2P resale framing</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    - README.md (current 132-line fraud-framed version -- understand structure, preserve Quick Start commands)
    - CLAUDE.md (current 34-line version -- preserve Hard Constraints section exactly)
    - .planning/phases/09-reframe-narrative/09-RESEARCH.md (terminology substitution map, ASCII diagram, README structure)
    - .planning/phases/09-reframe-narrative/09-CONTEXT.md (locked decisions for both files)
  </read_first>
  <action>
**README.md -- Full rewrite from scratch. Structure:**

1. Title: `# Ducket AI Galactica`
2. Badge: `[![License: Apache 2.0](https://img.shields.io/badge/License-Apache_2.0-blue.svg)](./LICENSE)`
3. Lead sentence: `Safe P2P ticket resale powered by AI and USDT escrow. Seller lists a ticket, buyer locks USDT, an autonomous AI agent verifies legitimacy, and a smart contract releases or refunds — no human in the loop.`
4. `## How It Works` -- 4 resale steps (NOT judging segments):
   - Step 1 "Seller Lists Ticket" -- seller posts event, section, quantity, price, face value to the platform
   - Step 2 "Buyer Locks USDT" -- buyer selects listing and deposits USDT into escrow via WDK non-custodial wallet on Sepolia
   - Step 3 "AI Agent Verifies" -- autonomous verification agent analyzes listing against market data, price caps, and cross-platform signals using Claude; produces a confidence score and detailed reasoning
   - Step 4 "Escrow Settles" -- smart contract releases USDT to seller (legitimate) or refunds buyer (failed verification) based on agent verdict
5. `## Architecture` -- Replace scraper pipeline diagram with the resale flow ASCII diagram from 09-RESEARCH.md (the Seller/Buyer/AI/Escrow diagram). Keep "FraudEscrow.sol" as the contract name since it is the actual filename.
6. `## Quick Start` -- Same prerequisites (Node >= 20, npm >= 9), same commands (`git clone`, `npm install`, `cp .env.example .env`). Update run commentary: `npm run demo` starts "AI verification agent + React dashboard simultaneously". Remove "polls StubHub/Viagogo/Facebook every 5 minutes" -- replace with "Agent verifies ticket listings against market data".
7. `## Project Structure` -- Same folder tree but update descriptions:
   - `agent/` → "AI verification agent"
   - `agent/src/` → "Core pipeline (verification loop, AI analysis, escrow settlement)"
   - `agent/tools/` → "Market data sources"
   - `agent/memory/` → "Listing records"
   - `agent/cases/` → "Verification evidence per listing"
   - `dashboard/` → "React dashboard (Vite + Tailwind v4)"
   - `dashboard/src/` → "Resale flow UI components"
   - `dashboard/server/` → "Express API serving listing and escrow data"
   - `contracts/` → "FraudEscrow.sol (Hardhat 3)"
8. `## Demo Video` -- Keep placeholder: `[VIDEO LINK — to be added before submission]`
9. `## Third-Party Disclosures` -- Same table but update Claude API purpose from "AI fraud classification" to "AI ticket verification". Update node-cron purpose from "Scan loop scheduling" to "Verification scheduling". All other rows stay the same.
10. `## Environment Variables` -- Keep: `See .env.example for all required variables with explaining comments.`
11. `## License` -- Keep: `Apache 2.0 — see [LICENSE](./LICENSE)`

Use generic seller/buyer language throughout -- NO Alice/Bob personas (those are reserved for demo script only).

**CLAUDE.md -- Partial rewrite. Structure stays the same, 4 sections:**

1. `## Project Overview` -- FULL REWRITE:
   ```
   Safe P2P ticket resale platform with USDT escrow enforcement for a hackathon.
   - **Core flow:** List → Lock USDT → AI Verify → Settle (no human triggers)
   - **Wallet:** WDK (mandatory) — escrow per listing, USDT release/refund
   - **License:** Apache 2.0
   - **Stack:** TypeScript/JavaScript
   ```

2. `## Decision Rules (Apply to EVERY technical choice)` -- FULL REWRITE of all 6 rules through P2P resale lens. Each rule keeps the `→` action directive format:
   - Rule 1: `Would a buyer trust the agent's verdict on this listing? → Add reasoning text visible in the Agent Decision Panel if not`
   - Rule 2: `Is WDK doing the wallet work? → Never route around it`  (unchanged -- already wallet-focused)
   - Rule 3: `Is the escrow flow programmable and conditional? → Must be agent-driven, seller never touches buyer funds`
   - Rule 4: `Can this resale flow be demoed in under 5 minutes? → Cut it if not`
   - Rule 5: `Is the repo clean enough for judges to follow the resale story? → Comment non-obvious logic`
   - Rule 6: `Architecture > polish when time is tight`  (unchanged -- generic enough)

3. `## Priorities (Judging Criteria Order)` -- UPDATE labels only, keep same 7-item order:
   - `1. Agent Intelligence (autonomous verification, explainable verdicts)`
   - `2. WDK Wallet Integration (mandatory, non-custodial escrow)`
   - `3. Technical Execution (clean resale architecture)`
   - `4. Agentic Payment Design (conditional escrow: release/refund/slash)`
   - `5. Originality`
   - `6. Polish & Shipability`
   - `7. Presentation & Demo`

4. `## Hard Constraints` -- NO CHANGES. Copy exactly as-is from current file.

**After writing both files, run banned-phrase grep:**
```bash
grep -cin "fraud detection\|monitoring tool\|scan and report" README.md CLAUDE.md
```
Must return 0 for each file. If any matches, fix them.

Note: The word "fraud" may appear in "FraudEscrow.sol" which is the actual contract filename -- this is acceptable in code-context references (project structure, contract path) but NOT in narrative descriptions.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && grep -cin "monitoring tool\|scan and report\|fraud detection" README.md CLAUDE.md && echo "FAIL: banned phrases found" || echo "PASS: no banned phrases" && grep -c "buyer locks\|seller lists\|AI verif" README.md && grep -c "buyer\|seller\|resale" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md first paragraph contains "Safe P2P ticket resale powered by AI and USDT escrow"
    - README.md contains "## How It Works" with 4 numbered steps (seller lists, buyer locks, AI verifies, escrow settles)
    - README.md architecture section contains ASCII diagram with "Seller" and "Buyer" and "AI Verification"
    - README.md Third-Party Disclosures table contains "AI ticket verification" (not "AI fraud classification")
    - README.md Quick Start section contains "npm run demo"
    - README.md does NOT contain "Alice" or "Bob"
    - CLAUDE.md Project Overview contains "Safe P2P ticket resale platform"
    - CLAUDE.md contains "List → Lock USDT → AI Verify → Settle"
    - CLAUDE.md Decision Rules contains "Would a buyer trust" in Rule 1
    - CLAUDE.md Hard Constraints section is unchanged (contains "WDK mandatory", "Apache 2.0", "Video demo")
    - CLAUDE.md Priorities contains "autonomous verification" in item 1
    - `grep -cin "fraud detection\|monitoring tool\|scan and report" README.md` returns 0
    - `grep -cin "fraud detection\|monitoring tool\|scan and report" CLAUDE.md` returns 0
    - `grep -c "buyer\|seller\|resale" CLAUDE.md` returns >= 6
  </acceptance_criteria>
  <done>README.md and CLAUDE.md fully rewritten with P2P resale framing. Zero banned phrases. All locked decisions from CONTEXT.md implemented. Judges reading either file see a safe P2P ticket resale platform, not a fraud monitoring tool.</done>
</task>

<task type="auto">
  <name>Task 2: Create demo script with Alice/Bob resale scenario</name>
  <files>docs/DEMO-SCRIPT.md</files>
  <read_first>
    - .planning/phases/09-reframe-narrative/09-RESEARCH.md (demo script structure template, banned phrases, canonical flow)
    - .planning/phases/09-reframe-narrative/09-CONTEXT.md (locked decisions for demo script)
    - .planning/ROADMAP.md (Phase 9 success criteria #3: "walks a named buyer/seller scenario mapped to actual dashboard screens")
    - README.md (the just-rewritten version -- ensure demo script terminology matches)
  </read_first>
  <action>
**Create new file `docs/DEMO-SCRIPT.md`** -- step-by-step demo walkthrough with Alice (seller) and Bob (buyer).

File content structure:

```markdown
# Ducket Demo Script -- Alice & Bob Resale Flow

> Step-by-step walkthrough for the 5-minute demo video. Each step maps to a dashboard screen.

## Before Recording

- Run `npm run demo` -- starts AI verification agent + React dashboard
- Open browser to `http://localhost:5173`
- Ensure `.env` has valid `ANTHROPIC_API_KEY` and `WDK_CLIENT_ID`
- Have a Sepolia-funded wallet ready (testnet USDT)

## Step 1: Alice Lists a FIFA Ticket

**Screen:** Seller Listing Form

**Narrate:** "Alice has a FIFA World Cup 2026 ticket she wants to sell. She fills in the event details and sets her asking price."

**Actions:**
1. Navigate to the Seller Listing Form
2. Fill in:
   - Event: FIFA World Cup 2026 -- Quarter Final
   - Section: Category 1 -- Lower Bowl
   - Quantity: 2
   - Price: $450 per ticket
   - Face Value: $350 per ticket
3. Submit the listing

**What judges see:** A new listing appears on the platform with Alice's ticket details and a "Pending Verification" status.

## Step 2: Bob Locks USDT in Escrow

**Screen:** Buyer Lock Screen

**Narrate:** "Bob finds Alice's listing and wants to buy. He locks USDT into escrow -- funds are held by the smart contract, not by Alice or the platform."

**Actions:**
1. Navigate to Alice's listing in the resale flow
2. Click "Lock USDT" to initiate escrow deposit
3. WDK wallet prompts Bob to approve the USDT transfer
4. Transaction confirms on Sepolia -- escrow address and Etherscan link displayed

**What judges see:** USDT moves from Bob's wallet to the FraudEscrow.sol contract. The UI shows the escrow transaction hash and a live Etherscan link. Funds are locked -- neither party can withdraw.

## Step 3: AI Agent Verifies the Listing

**Screen:** Agent Decision Panel

**Narrate:** "The AI agent autonomously analyzes Alice's listing. It checks the price against face value, looks for cross-platform signals, and produces a detailed verdict."

**Actions:**
1. The verification agent runs automatically (no manual trigger)
2. Agent Decision Panel expands to show:
   - Confidence score (e.g., 92%)
   - Classification category (e.g., "Legitimate" or "Price Cap Violation")
   - Full reasoning text (50+ words referencing specific listing fields)

**What judges see:** The Agent Decision Panel shows Claude's reasoning -- a multi-sentence explanation referencing Alice's price markup (28.6% above face value), event demand patterns, and cross-platform price consistency. The agent made this decision autonomously.

## Step 4: Escrow Settles

**Screen:** Settlement Outcome

**Narrate:** "Based on the AI verdict, the smart contract settles the escrow automatically. If the listing is legitimate, USDT releases to Alice. If verification fails, Bob gets a refund."

**Actions (Legitimate path):**
1. Settlement outcome displays: "Legitimate -- USDT Released to Seller"
2. On-chain transaction link shows the release transaction
3. Alice's wallet receives the USDT

**Actions (Failed verification path):**
1. Settlement outcome displays: "Failed Verification -- USDT Refunded to Buyer"
2. On-chain transaction link shows the refund transaction
3. Bob's wallet receives the USDT back

**What judges see:** The full lifecycle completes on-chain. The outcome label matches the agent's classification. Every step was autonomous -- no human approval was needed.

## Closing

**Narrate:** "Ducket AI Galactica makes P2P ticket resale safe. The buyer's funds are protected by escrow. The AI agent verifies every listing autonomously. And the smart contract enforces the outcome -- no trust required."

**Key points to emphasize:**
- Autonomous: AI agent runs without human triggers
- Non-custodial: WDK wallet -- buyer's USDT never held by the platform
- Explainable: Full AI reasoning visible in the Agent Decision Panel
- On-chain: Every escrow action has a verifiable transaction
```

**After writing, run banned-phrase grep:**
```bash
grep -cin "fraud monitoring\|fraud detection\|monitoring tool\|scan and report" docs/DEMO-SCRIPT.md
```
Must return 0. The word "fraud" should NOT appear anywhere in this file. "FraudEscrow.sol" is acceptable ONLY if referencing the contract by its technical filename.

Note: This demo script describes the FUTURE state after Phases 10-11 build the resale flow UI. Screen names (Seller Listing Form, Buyer Lock Screen, Agent Decision Panel, Settlement Outcome) are intentionally forward-looking. Phase 10-11 will implement these screens.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && test -f docs/DEMO-SCRIPT.md && echo "PASS: file exists" || echo "FAIL: file missing" && grep -c "Alice\|Bob" docs/DEMO-SCRIPT.md && grep -cin "fraud monitoring\|fraud detection\|monitoring tool\|scan and report" docs/DEMO-SCRIPT.md && echo "FAIL: banned phrases" || echo "PASS: no banned phrases"</automated>
  </verify>
  <acceptance_criteria>
    - docs/DEMO-SCRIPT.md exists and is non-empty
    - File contains "Alice" at least 5 times (seller persona throughout)
    - File contains "Bob" at least 5 times (buyer persona throughout)
    - File contains "## Step 1" through "## Step 4" (4 resale steps)
    - File contains "Seller Listing Form" (screen reference for Step 1)
    - File contains "Buyer Lock Screen" (screen reference for Step 2)
    - File contains "Agent Decision Panel" (screen reference for Step 3)
    - File contains "Settlement Outcome" (screen reference for Step 4)
    - File contains "npm run demo" in the setup section
    - `grep -cin "fraud monitoring\|fraud detection\|monitoring tool\|scan and report" docs/DEMO-SCRIPT.md` returns 0
    - File contains "FIFA" (specific event scenario, not generic)
    - File contains "50+ words" or demonstrates detailed reasoning example
  </acceptance_criteria>
  <done>Demo script created at docs/DEMO-SCRIPT.md with Alice/Bob personas walking 4 resale steps. Each step maps to a named dashboard screen. Zero banned phrases. Script is ready for Phase 13 rehearsals and video recording.</done>
</task>

</tasks>

<verification>
Run across all three target files after both tasks complete:

```bash
# Banned phrase check (must return 0 for each file)
grep -cin "fraud detection\|monitoring tool\|scan and report" README.md CLAUDE.md docs/DEMO-SCRIPT.md

# Required phrase check -- README
grep -c "Safe P2P ticket resale" README.md    # expect >= 1
grep -c "seller lists\|buyer locks\|AI verif\|escrow settles" README.md    # expect >= 4

# Required phrase check -- CLAUDE.md
grep -c "buyer\|seller\|resale" CLAUDE.md    # expect >= 6

# Required phrase check -- Demo script
grep -c "Alice" docs/DEMO-SCRIPT.md    # expect >= 5
grep -c "Bob" docs/DEMO-SCRIPT.md    # expect >= 5
```
</verification>

<success_criteria>
1. README.md uses "seller lists / buyer locks USDT / AI verifies / escrow settles" language throughout and contains no "monitoring tool" or "scan and report" framing
2. CLAUDE.md decision rules reference P2P resale context and buyer/seller agent role
3. Demo script walks Alice (seller) and Bob (buyer) through 4 resale steps mapped to named dashboard screens with no step that says "fraud monitoring"
4. Zero banned-phrase grep matches across all three files
</success_criteria>

<output>
After completion, create `.planning/phases/09-reframe-narrative/09-01-SUMMARY.md`
</output>
