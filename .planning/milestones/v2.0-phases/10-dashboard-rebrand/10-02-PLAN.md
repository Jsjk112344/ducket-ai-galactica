---
phase: 10-dashboard-rebrand
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - dashboard/src/App.tsx
  - dashboard/src/components/EscrowStatus.tsx
  - dashboard/src/components/ListingsTable.tsx
  - dashboard/src/components/AgentDecisionPanel.tsx
  - dashboard/src/components/WalletInspector.tsx
  - dashboard/src/components/TrustBadges.tsx
autonomous: false
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04

must_haves:
  truths:
    - "Dashboard header and cards show Ducket purple (#3D2870) and yellow (#F5C842), no default Tailwind slate/gray in primary UI areas"
    - "All headings render in Outfit Variable font"
    - "Trust badges (Price cap protected, Verified on-chain, Non-custodial) visible without scrolling on Listings tab"
    - "Active tab uses bg-brand-primary, not the old bg-accent indigo"
    - "Stat card values use text-brand-accent yellow, not old text-accent indigo"
    - "No text-gray-*, bg-gray-*, text-slate-*, bg-slate-* classes remain in primary UI component files"
  artifacts:
    - path: "dashboard/src/components/TrustBadges.tsx"
      provides: "3 trust badge pills for BRAND-04"
      exports: ["TrustBadges"]
      min_lines: 15
    - path: "dashboard/src/App.tsx"
      provides: "Rebranded header, tab bar, footer"
      contains: "bg-brand-primary"
    - path: "dashboard/src/components/EscrowStatus.tsx"
      provides: "Rebranded stat cards using shadcn Card"
      contains: "bg-brand-accent"
    - path: "dashboard/src/components/AgentDecisionPanel.tsx"
      provides: "Rebranded decision panel with purple borders"
      contains: "border-brand-primary"
    - path: "dashboard/src/components/WalletInspector.tsx"
      provides: "Rebranded wallet view with purple WDK badge"
      contains: "bg-brand-primary"
  key_links:
    - from: "dashboard/src/components/TrustBadges.tsx"
      to: "dashboard/src/components/ListingsTable.tsx"
      via: "import and render above table"
      pattern: "import.*TrustBadges"
    - from: "dashboard/src/components/EscrowStatus.tsx"
      to: "dashboard/src/components/ui/card.tsx"
      via: "import Card for stat cards"
      pattern: "import.*Card.*from.*ui/card"
---

<objective>
Apply Ducket rebrand to all 5 existing dashboard components: replace old indigo accent and gray classes with Ducket brand tokens, wire shadcn Card into EscrowStatus stat cards, create TrustBadges component, and update copy to P2P resale framing.

Purpose: Make the dashboard visually communicate the Ducket brand so judges see a polished product when they open localhost:5173.
Output: All components rebranded, TrustBadges visible on Listings tab, shadcn Card used in EscrowStatus, build passes, ready for visual verification.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard-rebrand/10-UI-SPEC.md
@.planning/phases/10-dashboard-rebrand/10-01-SUMMARY.md

<interfaces>
<!-- shadcn components available from Plan 01 -->
From dashboard/src/components/ui/card.tsx:
```typescript
export function Card({ className, ...props }: React.ComponentProps<"div">)
export function CardHeader({ className, ...props }: React.ComponentProps<"div">)
export function CardContent({ className, ...props }: React.ComponentProps<"div">)
export function CardTitle({ className, ...props }: React.ComponentProps<"div">)
export function CardDescription({ className, ...props }: React.ComponentProps<"p">)
```

<!-- Tailwind v4 tokens available from Plan 01 index.css -->
Brand tokens (use as Tailwind classes):
- `bg-bg-primary` = #0a0714 (dark purple page bg)
- `bg-bg-card` = #130f1f (card purple)
- `bg-brand-primary` = #3D2870 (primary purple — active tabs, badge bg tint)
- `text-brand-accent` / `bg-brand-accent` = #F5C842 (yellow — stat values, trust badge text, links)
- `text-muted-foreground` = hsl(263 20% 60%) (replaces all text-gray-400/500)
- `text-foreground` = hsl(0 0% 98%) (replaces text-white / text-gray-200/300)
- `border-border` = hsl(263 50% 18%) (replaces border-gray-700/800)
- `font-heading` = Outfit Variable (applied to h1-h6 via @layer base)

<!-- Existing component file paths and key lines to update -->
Accent collision remap table (from 10-UI-SPEC.md):
| File | Current class | New class |
|------|---------------|-----------|
| App.tsx ~42 | `bg-accent text-white` (active tab) | `bg-brand-primary text-white` |
| EscrowStatus.tsx ~21 | `text-accent` (stat value) | `text-brand-accent` |
| EscrowStatus.tsx ~50 | `bg-accent/20 text-accent` (Sepolia pill) | `bg-brand-primary/20 text-brand-accent` |
| AgentDecisionPanel.tsx ~33 | `border-accent/20` | `border-brand-primary/40` |
| AgentDecisionPanel.tsx ~76 | `border-accent/10` | `border-brand-primary/20` |
| AgentDecisionPanel.tsx ~83 | `text-accent` (tx link) | `text-brand-accent` |
| WalletInspector.tsx ~36 | `bg-accent text-white` (WDK badge) | `bg-brand-primary text-white` |
| ListingsTable.tsx ~112 | `border-accent/20` | `border-brand-primary/20` |

Gray class replacement: ALL `text-gray-*` and `bg-gray-*` occurrences must be replaced:
- `text-gray-400` -> `text-muted-foreground`
- `text-gray-500` -> `text-muted-foreground`
- `text-gray-300` -> `text-foreground`
- `text-gray-200` -> `text-foreground`
- `text-gray-600` -> `text-muted-foreground`
- `bg-gray-700` -> `bg-border` or `bg-bg-card`
- `border-gray-800` -> `border-border`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand App.tsx + create TrustBadges + update all 4 component files</name>
  <files>dashboard/src/App.tsx, dashboard/src/components/TrustBadges.tsx, dashboard/src/components/EscrowStatus.tsx, dashboard/src/components/ListingsTable.tsx, dashboard/src/components/AgentDecisionPanel.tsx, dashboard/src/components/WalletInspector.tsx</files>
  <read_first>
    - dashboard/src/App.tsx
    - dashboard/src/components/EscrowStatus.tsx
    - dashboard/src/components/ListingsTable.tsx
    - dashboard/src/components/AgentDecisionPanel.tsx
    - dashboard/src/components/WalletInspector.tsx
    - dashboard/src/components/ui/card.tsx
    - .planning/phases/10-dashboard-rebrand/10-UI-SPEC.md
  </read_first>
  <action>
**A. Create `dashboard/src/components/TrustBadges.tsx`:**

```tsx
// Ducket AI Galactica — Trust Badges
// Horizontal strip of trust indicators for P2P resale safety.
// Rendered above ListingsTable on the Listings tab (BRAND-04).
// Apache 2.0 License

import { Shield, CheckCircle, Lock } from 'lucide-react';

const TRUST_BADGES = [
  { label: 'Price cap protected', Icon: Shield },
  { label: 'Verified on-chain', Icon: CheckCircle },
  { label: 'Non-custodial', Icon: Lock },
] as const;

export function TrustBadges() {
  return (
    <div className="flex flex-wrap gap-2 mb-4">
      {TRUST_BADGES.map(({ label, Icon }) => (
        <span
          key={label}
          className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full
                     bg-brand-primary/20 text-brand-accent border border-brand-primary/40
                     text-xs font-medium"
        >
          <Icon className="w-3.5 h-3.5" />
          {label}
        </span>
      ))}
    </div>
  );
}
```

**B. Update `dashboard/src/App.tsx`:**

Apply these exact changes:

1. Update the h1 text from `"Ducket AI Galactica"` to `"Ducket"`
2. Update the subtitle from `"Autonomous Fraud Detection Agent"` to `"Safe P2P ticket resale — buyer protected by escrow"`
3. Change subtitle class from `text-gray-400` to `text-muted-foreground`
4. Change active tab class from `bg-accent text-white` to `bg-brand-primary text-white`
5. Change inactive tab `text-gray-400` to `text-muted-foreground`
6. Change inactive tab `hover:text-gray-200` to `hover:text-foreground`
7. Change timestamp `text-gray-500` to `text-muted-foreground`
8. Change footer `text-gray-600` to `text-muted-foreground`

The full active tab ternary becomes:
```
activeTab === tab.id
  ? 'bg-brand-primary text-white'
  : 'bg-bg-card text-muted-foreground hover:text-foreground'
```

**C. Update `dashboard/src/components/EscrowStatus.tsx`:**

1. Import shadcn Card: add `import { Card, CardContent } from './ui/card';` at top
2. In `StatCard` component, replace the outer `<div className="bg-bg-card rounded-lg p-4">` with `<Card><CardContent className="p-4">...</CardContent></Card>`
3. Change stat value `text-accent` to `text-brand-accent`
4. Change label `text-gray-400` to `text-muted-foreground`
5. Change "Escrow Contract" label `text-gray-500` to `text-muted-foreground`
6. Change contract address `text-gray-300` to `text-foreground`
7. Change Sepolia pill from `bg-accent/20 text-accent` to `bg-brand-primary/20 text-brand-accent`
8. Change empty state `text-gray-500` to `text-muted-foreground`

Updated StatCard:
```tsx
function StatCard({ label, value }: StatCardProps) {
  return (
    <Card>
      <CardContent className="p-4">
        <p className="text-2xl font-bold text-brand-accent">{value}</p>
        <p className="text-muted-foreground text-sm mt-1">{label}</p>
      </CardContent>
    </Card>
  );
}
```

**D. Update `dashboard/src/components/ListingsTable.tsx`:**

1. Add import: `import { TrustBadges } from './TrustBadges';`
2. Render `<TrustBadges />` immediately BEFORE the `<div className="overflow-x-auto rounded-lg">` — inside the returned JSX, wrap both in a fragment or outer div so TrustBadges appears above the table
3. Change table header `text-gray-400` to `text-muted-foreground`
4. Change row border `border-gray-800` to `border-border`
5. Change seller text `text-gray-300` to `text-foreground`
6. Change face value `text-gray-400` to `text-muted-foreground`
7. Change status `text-gray-400` to `text-muted-foreground`
8. Change expanded row border from `border-accent/20` to `border-brand-primary/20`
9. Change empty state `text-gray-500` to `text-muted-foreground`
10. Change source badge `bg-gray-700 text-gray-400` to `bg-bg-card text-muted-foreground`

Return structure should be:
```tsx
return (
  <>
    <TrustBadges />
    <div className="overflow-x-auto rounded-lg">
      <table ...>
      ...
      </table>
    </div>
  </>
);
```

**E. Update `dashboard/src/components/AgentDecisionPanel.tsx`:**

1. Change outer border `border-accent/20` to `border-brand-primary/40`
2. Change all `text-gray-500` labels to `text-muted-foreground`
3. Change reasoning `text-gray-300` to `text-foreground`
4. Change classification source `text-gray-400` to `text-muted-foreground`
5. Change action color fallback `text-gray-300` to `text-foreground`
6. Change etherscan border `border-accent/10` to `border-brand-primary/20`
7. Change etherscan link `text-accent` to `text-brand-accent`

**F. Update `dashboard/src/components/WalletInspector.tsx`:**

1. Change all `text-gray-500` labels to `text-muted-foreground`
2. Change wallet address `text-gray-300` to `text-foreground`
3. Change WDK badge from `bg-accent text-white` to `bg-brand-primary text-white`
4. Change escrow contract `text-gray-400` to `text-muted-foreground`
5. Change loading state `text-gray-500` to `text-muted-foreground`

**IMPORTANT: Do NOT touch `dashboard/src/components/Badge.tsx`** — this is the classification badge with custom color maps (SCALPING_VIOLATION, LEGITIMATE, etc.) and must remain as-is.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && grep -r "text-gray-\|bg-gray-\|text-slate-\|bg-slate-" dashboard/src/App.tsx dashboard/src/components/EscrowStatus.tsx dashboard/src/components/ListingsTable.tsx dashboard/src/components/AgentDecisionPanel.tsx dashboard/src/components/WalletInspector.tsx && echo "FAIL: gray/slate classes remain" || echo "PASS: no gray/slate classes" && cd dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/src/App.tsx contains `bg-brand-primary text-white` (active tab)
    - dashboard/src/App.tsx contains `"Ducket"` as h1 text (not "Ducket AI Galactica")
    - dashboard/src/App.tsx contains `Safe P2P ticket resale`
    - dashboard/src/App.tsx does NOT contain `text-gray-` or `bg-accent text-white`
    - dashboard/src/components/TrustBadges.tsx exists and contains `Price cap protected`
    - dashboard/src/components/TrustBadges.tsx contains `Verified on-chain`
    - dashboard/src/components/TrustBadges.tsx contains `Non-custodial`
    - dashboard/src/components/TrustBadges.tsx contains `bg-brand-primary/20 text-brand-accent`
    - dashboard/src/components/ListingsTable.tsx contains `import { TrustBadges }` or `import { TrustBadges } from`
    - dashboard/src/components/ListingsTable.tsx contains `<TrustBadges`
    - dashboard/src/components/ListingsTable.tsx does NOT contain `border-accent/20`
    - dashboard/src/components/EscrowStatus.tsx contains `import { Card` or `import { Card,`
    - dashboard/src/components/EscrowStatus.tsx contains `text-brand-accent` (stat values)
    - dashboard/src/components/EscrowStatus.tsx does NOT contain `text-accent` as a standalone class
    - dashboard/src/components/AgentDecisionPanel.tsx contains `border-brand-primary/40`
    - dashboard/src/components/AgentDecisionPanel.tsx contains `text-brand-accent` (tx link)
    - dashboard/src/components/AgentDecisionPanel.tsx does NOT contain `border-accent/20` or `text-accent`
    - dashboard/src/components/WalletInspector.tsx contains `bg-brand-primary text-white` (WDK badge)
    - dashboard/src/components/WalletInspector.tsx does NOT contain `bg-accent text-white`
    - No file among App.tsx, EscrowStatus.tsx, ListingsTable.tsx, AgentDecisionPanel.tsx, WalletInspector.tsx contains `text-gray-` or `bg-gray-`
    - dashboard/src/components/Badge.tsx is UNCHANGED (classification badge kept as-is)
    - `cd dashboard && npm run build` exits 0
  </acceptance_criteria>
  <done>All 5 existing components rebranded with Ducket tokens, TrustBadges created and wired into ListingsTable, shadcn Card used in EscrowStatus stat cards, no gray/slate/old-accent classes remain in primary component files, build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Ducket rebrand</name>
  <files>dashboard/src/App.tsx, dashboard/src/components/TrustBadges.tsx, dashboard/src/components/EscrowStatus.tsx, dashboard/src/components/ListingsTable.tsx</files>
  <action>
Start the dev server and visually verify all 4 BRAND requirements are met. The executor runs `cd dashboard && npm run dev`, then the user checks the browser at localhost:5173. This is a human-verify checkpoint — no code changes, only visual confirmation.
  </action>
  <what-built>
    Complete Ducket brand rebrand of the dashboard:
    - Purple/yellow color scheme (#3D2870 / #F5C842) on dark purple background (#0a0714)
    - Outfit Variable font on all headings, Inter Variable on body
    - Trust badges ("Price cap protected", "Verified on-chain", "Non-custodial") above listings table
    - shadcn Card components in EscrowStatus stat cards
    - Active tab bar uses brand purple, stat values use brand yellow
    - All gray classes replaced with muted-foreground tokens
  </what-built>
  <how-to-verify>
    1. Run `cd dashboard && npm run dev` to start the dev server
    2. Open http://localhost:5173 in browser
    3. Verify BRAND-01: Header and cards use purple tones, yellow accent on stat values. No default gray/slate palette visible in primary areas
    4. Verify BRAND-02: Open DevTools -> select h1 "Ducket" -> Computed tab -> font-family should show "Outfit Variable". Body text should show "Inter Variable"
    5. Verify BRAND-03: Stat cards in Escrow tab render with rounded shadcn Card styling
    6. Verify BRAND-04: On Listings tab, 3 trust badges ("Price cap protected", "Verified on-chain", "Non-custodial") are visible above the table WITHOUT scrolling
    7. Click each tab (Listings, Escrow, Wallet) — active tab should have purple background, not yellow
    8. If listings exist, click a row to expand Agent Decision Panel — border should be purple, tx link should be yellow
  </how-to-verify>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica/dashboard && npm run build</automated>
  </verify>
  <done>User has visually confirmed all 4 BRAND requirements are met in the browser.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd dashboard && npm run build` exits 0
2. `grep -r "fonts.googleapis.com" dashboard/src/` returns no matches (BRAND-02)
3. `ls dashboard/tailwind.config.js 2>/dev/null && echo "FAIL" || echo "PASS"` returns PASS (BRAND-03)
4. `grep -r "text-gray-\|bg-gray-\|text-slate-\|bg-slate-" dashboard/src/App.tsx dashboard/src/components/EscrowStatus.tsx dashboard/src/components/ListingsTable.tsx dashboard/src/components/AgentDecisionPanel.tsx dashboard/src/components/WalletInspector.tsx` returns no matches (BRAND-01)
5. `grep "Price cap protected" dashboard/src/components/TrustBadges.tsx` returns a match (BRAND-04)
6. Visual verification confirms Ducket purple/yellow theme renders correctly
</verification>

<success_criteria>
- Dashboard header shows "Ducket" in Outfit Variable font with "Safe P2P ticket resale" subtitle
- Active tab uses bg-brand-primary purple, stat values use text-brand-accent yellow
- Trust badges visible without scrolling on Listings tab
- EscrowStatus uses shadcn Card for stat cards
- No text-gray-*, bg-gray-*, text-slate-*, bg-slate-* in primary component files
- No bg-accent or text-accent (old indigo) in primary component files
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-rebrand/10-02-SUMMARY.md`
</output>
