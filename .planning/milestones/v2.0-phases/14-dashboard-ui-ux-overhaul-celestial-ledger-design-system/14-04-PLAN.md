---
phase: 14-dashboard-ui-ux-overhaul-celestial-ledger-design-system
plan: 04
type: execute
wave: 3
depends_on: [14-03]
files_modified:
  - .planning/REQUIREMENTS.md
  - dashboard/src/components/ListingForm.tsx
  - dashboard/src/components/BuyerLockStep.tsx
  - dashboard/src/components/SettleStep.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08, UI-09, UI-10]
gap_closure: true

must_haves:
  truths:
    - "UI-01 through UI-10 are formally defined in REQUIREMENTS.md with descriptions"
    - "Phase 14 has 10 rows in the REQUIREMENTS.md traceability table"
    - "ListingForm.tsx uses bg-m3-surface-container div instead of Card/CardContent wrapper"
    - "BuyerLockStep.tsx uses bg-m3-surface-container div instead of Card/CardContent wrapper"
    - "SettleStep.tsx uses bg-m3-surface-container div instead of Card/CardContent wrapper"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "UI-01 through UI-10 formal definitions and Phase 14 traceability rows"
      contains: "UI-01"
    - path: "dashboard/src/components/ListingForm.tsx"
      provides: "M3 surface container wrapper"
      contains: "bg-m3-surface-container"
    - path: "dashboard/src/components/BuyerLockStep.tsx"
      provides: "M3 surface container wrapper"
      contains: "bg-m3-surface-container"
    - path: "dashboard/src/components/SettleStep.tsx"
      provides: "M3 surface container wrapper"
      contains: "bg-m3-surface-container"
  key_links:
    - from: "dashboard/src/components/ListingForm.tsx"
      to: "dashboard/src/index.css"
      via: "bg-m3-surface-container Tailwind utility"
      pattern: "bg-m3-surface-container"
---

<objective>
Close two verification gaps from Phase 14: (1) register UI-01 through UI-10 requirement definitions and traceability rows in REQUIREMENTS.md, and (2) replace Card/CardContent outer wrappers in ListingForm, BuyerLockStep, and SettleStep with plain M3 surface container divs.

Purpose: Verification found 13/15 truths passing. These two gaps close the remaining failures — one documentation, one partial migration.
Output: Updated REQUIREMENTS.md with UI-* definitions, three components with M3 surface wrappers replacing shadcn Card.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-dashboard-ui-ux-overhaul-celestial-ledger-design-system/14-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UI-01 through UI-10 requirement definitions and Phase 14 traceability rows to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current requirements file — must preserve all existing content)
    - .planning/ROADMAP.md (Phase 14 section — source of requirement descriptions)
    - .planning/phases/14-dashboard-ui-ux-overhaul-celestial-ledger-design-system/14-VERIFICATION.md (requirements coverage table — source of requirement descriptions)
  </read_first>
  <action>
1. Add a new `### UI/UX Overhaul` section to `.planning/REQUIREMENTS.md` AFTER the `### Demo` section (line 33) and BEFORE `## v1.0 Requirements` (line 36). Insert these definitions:

```markdown
### UI/UX Overhaul

- [x] **UI-01**: M3 dark token system (19 CSS vars in :root + 19 @theme mappings) with JetBrains Mono as font-mono
- [x] **UI-02**: Fixed top nav with Ducket logomark, tab links, and v2.0 badge visible at all scroll positions
- [x] **UI-03**: Hero section with Logo_2.png, "Celestial Ledger" title, Protocol v2.0 badge, and animated node status
- [x] **UI-04**: FAB button (gold, bottom-right) visible on all tabs, navigates to Resale Flow
- [x] **UI-05**: Stat cards with border-l-4 color coding (primary/secondary/tertiary/error) and progress bars
- [x] **UI-06**: Active Order Book table header replacing plain listings table
- [x] **UI-07**: M3 surface/outline row styling with hover states in listings table
- [x] **UI-08**: M3 semantic badge colors (error-container for scalping, tertiary for legitimate) and glass panel AgentDecisionPanel
- [x] **UI-09**: Chevron expand column with rotate-180 animation in listings table
- [x] **UI-10**: All detail components migrated to M3 tokens (WalletInspector, ResaleFlowPanel, TrustBadges, resale steps, EtherscanLink)
```

2. Add 10 Phase 14 rows to the traceability table. Insert BEFORE the empty line after `| DEMO-03 | Phase 13 | Pending |` (line 73). Add:

```markdown
| UI-01 | Phase 14 | Complete |
| UI-02 | Phase 14 | Complete |
| UI-03 | Phase 14 | Complete |
| UI-04 | Phase 14 | Complete |
| UI-05 | Phase 14 | Complete |
| UI-06 | Phase 14 | Complete |
| UI-07 | Phase 14 | Complete |
| UI-08 | Phase 14 | Complete |
| UI-09 | Phase 14 | Complete |
| UI-10 | Phase 14 | Complete |
```

3. Update the `**Coverage:**` section to reflect the new totals:
```markdown
**Coverage:**
- v2.0 requirements: 24 total
- Mapped to phases: 24
- Unmapped: 0
```
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && grep "UI-01" .planning/REQUIREMENTS.md && grep "UI-10" .planning/REQUIREMENTS.md && grep -c "UI-0" .planning/REQUIREMENTS.md && grep "| UI-01 | Phase 14 | Complete |" .planning/REQUIREMENTS.md && grep "| UI-10 | Phase 14 | Complete |" .planning/REQUIREMENTS.md && grep "24 total" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains `### UI/UX Overhaul` section header
    - .planning/REQUIREMENTS.md contains `**UI-01**: M3 dark token system`
    - .planning/REQUIREMENTS.md contains `**UI-02**: Fixed top nav`
    - .planning/REQUIREMENTS.md contains `**UI-03**: Hero section`
    - .planning/REQUIREMENTS.md contains `**UI-04**: FAB button`
    - .planning/REQUIREMENTS.md contains `**UI-05**: Stat cards with border-l-4`
    - .planning/REQUIREMENTS.md contains `**UI-06**: Active Order Book`
    - .planning/REQUIREMENTS.md contains `**UI-07**: M3 surface/outline row styling`
    - .planning/REQUIREMENTS.md contains `**UI-08**: M3 semantic badge colors`
    - .planning/REQUIREMENTS.md contains `**UI-09**: Chevron expand column`
    - .planning/REQUIREMENTS.md contains `**UI-10**: All detail components migrated`
    - .planning/REQUIREMENTS.md contains `| UI-01 | Phase 14 | Complete |`
    - .planning/REQUIREMENTS.md contains `| UI-10 | Phase 14 | Complete |`
    - .planning/REQUIREMENTS.md contains `v2.0 requirements: 24 total`
    - .planning/REQUIREMENTS.md still contains all existing sections (Narrative, Branding, Resale Flow, Demo)
  </acceptance_criteria>
  <done>All 10 UI-* requirement IDs formally defined in REQUIREMENTS.md with descriptions, all 10 mapped to Phase 14 in traceability table, coverage count updated to 24</done>
</task>

<task type="auto">
  <name>Task 2: Replace Card/CardContent wrappers with M3 surface container divs in ListingForm, BuyerLockStep, SettleStep</name>
  <files>dashboard/src/components/ListingForm.tsx, dashboard/src/components/BuyerLockStep.tsx, dashboard/src/components/SettleStep.tsx</files>
  <read_first>
    - dashboard/src/components/ListingForm.tsx (current file — Card wrapper on lines 34 and 94)
    - dashboard/src/components/BuyerLockStep.tsx (current file — Card wrapper on lines 31 and 59)
    - dashboard/src/components/SettleStep.tsx (current file — Card wrapper on lines 51 and 69)
  </read_first>
  <action>
**ListingForm.tsx:**
1. Remove the import `import { Card, CardContent } from './ui/card';` (line 9)
2. Replace `<Card>` (line 34) with `<div className="bg-m3-surface-container rounded-xl p-4">`
3. Replace `<CardContent className="p-4">` (line 35) — remove this wrapper entirely (the p-4 is now on the outer div)
4. Replace `</CardContent>` (line 93) — remove
5. Replace `</Card>` (line 94) with `</div>`

The resulting outer structure should be:
```tsx
return (
  <div className="bg-m3-surface-container rounded-xl p-4">
    <h2 className="text-lg font-heading font-semibold text-m3-on-surface mb-4">List a Ticket</h2>
    <form onSubmit={handleSubmit} className="grid grid-cols-2 gap-4">
      ...
    </form>
  </div>
);
```

**BuyerLockStep.tsx:**
1. Remove the import `import { Card, CardContent } from './ui/card';` (line 10)
2. Replace `<Card>` (line 31) with `<div className="bg-m3-surface-container rounded-xl p-4 space-y-4">`
3. Replace `<CardContent className="p-4 space-y-4">` (line 32) — remove this wrapper entirely (p-4 and space-y-4 are now on the outer div)
4. Replace `</CardContent>` (line 58) — remove
5. Replace `</Card>` (line 59) with `</div>`

**SettleStep.tsx:**
1. Remove the import `import { Card, CardContent } from './ui/card';` (line 7)
2. Replace `<Card>` (line 51) with `<div className="bg-m3-surface-container rounded-xl p-6 space-y-4">`
3. Replace `<CardContent className="p-6 space-y-4">` (line 52) — remove this wrapper entirely (p-6 and space-y-4 are now on the outer div)
4. Replace `</CardContent>` (line 68) — remove
5. Replace `</Card>` (line 69) with `</div>`

CRITICAL: Preserve ALL existing logic, props, state, form handling, OUTCOME_CONFIG, and conditional rendering. Only the outermost wrapper element changes.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && grep "bg-m3-surface-container" dashboard/src/components/ListingForm.tsx && grep "bg-m3-surface-container" dashboard/src/components/BuyerLockStep.tsx && grep "bg-m3-surface-container" dashboard/src/components/SettleStep.tsx && ! grep "import.*Card.*from.*ui/card" dashboard/src/components/ListingForm.tsx && ! grep "import.*Card.*from.*ui/card" dashboard/src/components/BuyerLockStep.tsx && ! grep "import.*Card.*from.*ui/card" dashboard/src/components/SettleStep.tsx && grep "OUTCOME_CONFIG" dashboard/src/components/SettleStep.tsx && grep ".includes(" dashboard/src/components/SettleStep.tsx && cd dashboard && npx vite build --mode development 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/src/components/ListingForm.tsx contains `bg-m3-surface-container rounded-xl p-4`
    - dashboard/src/components/ListingForm.tsx does NOT contain `import { Card` or `import { CardContent`
    - dashboard/src/components/ListingForm.tsx does NOT contain `<Card>` or `<CardContent`
    - dashboard/src/components/ListingForm.tsx still contains `onSubmit={handleSubmit}`
    - dashboard/src/components/ListingForm.tsx still contains `text-m3-on-surface` on the h2
    - dashboard/src/components/BuyerLockStep.tsx contains `bg-m3-surface-container rounded-xl p-4 space-y-4`
    - dashboard/src/components/BuyerLockStep.tsx does NOT contain `import { Card` or `<Card>`
    - dashboard/src/components/BuyerLockStep.tsx still contains `onLock` and `onAdvance` prop usage
    - dashboard/src/components/SettleStep.tsx contains `bg-m3-surface-container rounded-xl p-6 space-y-4`
    - dashboard/src/components/SettleStep.tsx does NOT contain `import { Card` or `<Card>`
    - dashboard/src/components/SettleStep.tsx still contains `OUTCOME_CONFIG` object
    - dashboard/src/components/SettleStep.tsx still contains `.includes('release')` and `.includes('refund')` and `.includes('slash')`
    - `cd dashboard && npx vite build` exits 0
  </acceptance_criteria>
  <done>All three resale step components use plain M3 surface container divs. No Card/CardContent imports remain. All logic preserved. Vite build passes.</done>
</task>

</tasks>

<verification>
- `grep -c "UI-0" .planning/REQUIREMENTS.md` returns 20+ (10 definitions + 10 traceability rows)
- `grep "bg-m3-surface-container" dashboard/src/components/ListingForm.tsx` matches
- `grep "bg-m3-surface-container" dashboard/src/components/BuyerLockStep.tsx` matches
- `grep "bg-m3-surface-container" dashboard/src/components/SettleStep.tsx` matches
- `! grep "import.*Card.*from.*ui/card" dashboard/src/components/ListingForm.tsx` passes (no Card import)
- `cd dashboard && npx vite build` exits 0
</verification>

<success_criteria>
REQUIREMENTS.md has formal UI-01 through UI-10 definitions with Phase 14 traceability rows. ListingForm, BuyerLockStep, and SettleStep use bg-m3-surface-container rounded-xl divs instead of shadcn Card/CardContent wrappers. All logic and data handling preserved. Vite build green. Both verification gaps from 14-VERIFICATION.md resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/14-dashboard-ui-ux-overhaul-celestial-ledger-design-system/14-04-SUMMARY.md`
</output>
