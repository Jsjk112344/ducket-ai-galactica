---
phase: 02-wdk-wallet-escrow-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/package.json
  - agent/src/wallet/index.ts
  - agent/src/wallet/types.ts
  - agent/src/index.ts
  - agent/tools/wallet-smoke-test.js
  - .env.example
autonomous: true
requirements: [WLLT-01, WLLT-02, WLLT-03, WLLT-04]
user_setup:
  - service: infura
    why: "Sepolia RPC endpoint for on-chain wallet operations"
    env_vars:
      - name: SEPOLIA_RPC_URL
        source: "Infura Dashboard -> Create Project -> Sepolia endpoint URL"
  - service: wallet-seed
    why: "BIP-39 mnemonic for deterministic wallet derivation"
    env_vars:
      - name: ESCROW_WALLET_SEED
        source: "Generate with: node -e \"console.log(require('bip39').generateMnemonic())\""

must_haves:
  truths:
    - "Agent creates a deterministic wallet address from ESCROW_WALLET_SEED env var"
    - "Agent refuses to start if ESCROW_WALLET_SEED is missing"
    - "Same seed phrase always produces the same wallet address across restarts"
    - "Private key is never logged or written to disk"
    - "Wallet operations use WDK exclusively — no raw ethers.Wallet usage"
  artifacts:
    - path: "agent/src/wallet/index.ts"
      provides: "WDK wallet singleton with startup guard"
      exports: ["getWallet", "getAccount", "dispose"]
    - path: "agent/src/wallet/types.ts"
      provides: "WalletConfig and TransferResult type definitions"
      exports: ["WalletConfig", "TransferResult"]
    - path: "agent/tools/wallet-smoke-test.js"
      provides: "Standalone smoke test: generate wallet, check balance, self-send"
      contains: "SMOKE TEST PASSED"
  key_links:
    - from: "agent/src/wallet/index.ts"
      to: "@tetherto/wdk-wallet-evm"
      via: "WalletManagerEvm constructor"
      pattern: "new WalletManagerEvm"
    - from: "agent/src/wallet/index.ts"
      to: "process.env.ESCROW_WALLET_SEED"
      via: "startup guard"
      pattern: "ESCROW_WALLET_SEED"
    - from: "agent/src/index.ts"
      to: "agent/src/wallet/index.ts"
      via: "import getWallet"
      pattern: "from.*wallet"
---

<objective>
WDK wallet module with singleton pattern, startup guard, and smoke test script.

Purpose: WLLT-01 through WLLT-04 require a self-custodial WDK wallet that survives restarts, never exposes private keys, and can transact on Sepolia. This plan creates the wallet module the entire agent depends on.
Output: `agent/src/wallet/` module with getWallet/getAccount/dispose exports, smoke test at `agent/tools/wallet-smoke-test.js`
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wdk-wallet-escrow-contract/02-CONTEXT.md
@.planning/phases/02-wdk-wallet-escrow-contract/02-RESEARCH.md

<interfaces>
<!-- Existing codebase patterns from Phase 1 -->

From agent/package.json:
```json
{
  "name": "@ducket/agent",
  "scripts": { "dev": "tsx src/index.ts", "build": "tsc", "start": "node dist/index.js" },
  "dependencies": {},
  "devDependencies": { "@types/node": "^22.0.0", "tsx": "^4.0.0" }
}
```

From .env.example (env vars already declared):
```
ESCROW_WALLET_SEED=twelve word seed phrase here never commit real seed
WDK_API_KEY=your_wdk_api_key_here
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/your_project_id
SEPOLIA_USDT_CONTRACT=0x7169d38820dfd117c3fa1f22a697dba58d90ba06
```

From agent/src/index.ts (placeholder):
```typescript
console.log("Ducket Agent — not yet implemented. See ROADMAP Phase 2+.");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install WDK dependencies and create wallet module with types</name>
  <files>agent/package.json, agent/src/wallet/types.ts, agent/src/wallet/index.ts, agent/src/index.ts, .env.example</files>
  <read_first>
    - agent/package.json (current dependencies)
    - agent/src/index.ts (current placeholder entry point)
    - .env.example (current env var declarations)
    - .planning/phases/02-wdk-wallet-escrow-contract/02-RESEARCH.md (WDK API patterns)
  </read_first>
  <action>
1. Install dependencies in agent workspace:
   ```bash
   cd agent && npm install @tetherto/wdk-wallet-evm@1.0.0-beta.8 ethers@6.16.0 dotenv@17.3.1
   ```

2. Create `agent/src/wallet/types.ts` with these exact exports:
   ```typescript
   export interface WalletConfig {
     seedPhrase: string;
     providerUrl: string;
     usdtContractAddress: string;
   }

   export interface TransferResult {
     hash: string;
     from: string;
     to: string;
     amount: string;
   }
   ```

3. Create `agent/src/wallet/index.ts` implementing the WDK singleton pattern:
   - Import `WalletManagerEvm` from `@tetherto/wdk-wallet-evm`
   - Import `dotenv/config` for env loading
   - Private module-level `_wallet` variable (initially null)
   - `getWallet()` function that:
     - Throws `Error('ESCROW_WALLET_SEED env var is required — refusing to start')` if env var missing
     - Throws `Error('SEPOLIA_RPC_URL env var is required')` if env var missing
     - Creates `new WalletManagerEvm(process.env.ESCROW_WALLET_SEED, { provider: process.env.SEPOLIA_RPC_URL })` on first call
     - Returns cached `_wallet` on subsequent calls
   - `getAccount(index = 0)` async function that calls `getWallet().getAccount(index)` and returns the account
   - `dispose()` function that calls `_wallet?.dispose()` and sets `_wallet = null`
   - Register `process.on('exit', dispose)` at module level
   - Export `{ getWallet, getAccount, dispose }` and re-export types from `./types`

4. Update `agent/src/index.ts` to replace placeholder with startup guard:
   ```typescript
   import 'dotenv/config';
   import { getWallet, getAccount } from './wallet/index.js';

   async function main() {
     console.log('Ducket Agent starting...');

     // Startup guard — fails fast if wallet env vars missing
     const wallet = getWallet();
     const account = await getAccount();
     const address = await account.getAddress();
     console.log('Agent wallet address:', address);
     console.log('Ducket Agent ready.');
   }

   main().catch((err) => {
     console.error('Agent startup failed:', err.message);
     process.exit(1);
   });
   ```

5. Add `SEPOLIA_DEPLOYER_PRIVATE_KEY` to `.env.example` (needed by Plan 02 for Hardhat deploy):
   ```
   SEPOLIA_DEPLOYER_PRIVATE_KEY=0xyour_deployer_private_key_here
   ```
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && grep -q "wdk-wallet-evm" agent/package.json && grep -q "getWallet" agent/src/wallet/index.ts && grep -q "ESCROW_WALLET_SEED" agent/src/wallet/index.ts && grep -q "WalletConfig" agent/src/wallet/types.ts && grep -q "from.*wallet" agent/src/index.ts && echo "VERIFY PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `agent/package.json` contains `"@tetherto/wdk-wallet-evm": "1.0.0-beta.8"` in dependencies
    - `agent/package.json` contains `"ethers": "6.16.0"` in dependencies
    - `agent/package.json` contains `"dotenv"` in dependencies
    - `agent/src/wallet/types.ts` exports `WalletConfig` and `TransferResult` interfaces
    - `agent/src/wallet/index.ts` contains `new WalletManagerEvm` constructor call
    - `agent/src/wallet/index.ts` contains string `ESCROW_WALLET_SEED env var is required`
    - `agent/src/wallet/index.ts` contains string `SEPOLIA_RPC_URL env var is required`
    - `agent/src/wallet/index.ts` exports `getWallet`, `getAccount`, `dispose`
    - `agent/src/wallet/index.ts` contains `process.on('exit', dispose)`
    - `agent/src/index.ts` imports from `./wallet/index.js`
    - `agent/src/index.ts` calls `getWallet()` and logs wallet address
    - `.env.example` contains `SEPOLIA_DEPLOYER_PRIVATE_KEY`
    - No occurrences of `new ethers.Wallet(` in agent/src/ (WDK only)
    - No occurrences of private key, mnemonic, or seed in any console.log statement in agent/src/
  </acceptance_criteria>
  <done>WDK wallet module installed and implemented with singleton pattern, startup guard that refuses to start without ESCROW_WALLET_SEED and SEPOLIA_RPC_URL, dispose cleanup on exit, and agent entry point wired to wallet</done>
</task>

<task type="auto">
  <name>Task 2: Create wallet smoke test script</name>
  <files>agent/tools/wallet-smoke-test.js</files>
  <read_first>
    - agent/src/wallet/index.ts (wallet module just created — use its exports)
    - .planning/phases/02-wdk-wallet-escrow-contract/02-RESEARCH.md (smoke test pattern and anti-patterns)
  </read_first>
  <action>
Create `agent/tools/wallet-smoke-test.js` as a standalone ESM script (not TypeScript — judges run it directly with `node`).

The script must:
1. Import dotenv/config for env loading (path: `../.env` relative or rely on root .env)
2. Import WalletManagerEvm from `@tetherto/wdk-wallet-evm`
3. Check `ESCROW_WALLET_SEED` env var exists, throw if missing
4. Check `SEPOLIA_RPC_URL` env var exists, throw if missing
5. Check `SEPOLIA_USDT_CONTRACT` env var exists, throw if missing
6. Create `new WalletManagerEvm(process.env.ESCROW_WALLET_SEED, { provider: process.env.SEPOLIA_RPC_URL })`
7. Get account 0: `await wallet.getAccount(0)`
8. Get address: `await account.getAddress()`
9. Log `Wallet address: ${address}` (safe — address is public)
10. Read USDT balance: `await account.getTokenBalance(process.env.SEPOLIA_USDT_CONTRACT)`
11. Log `USDT balance: ${balance.toString()}`
12. Send 0 ETH to self (NOT 0 USDT — per research anti-pattern): `await account.sendTransaction({ to: address, value: 0n })`
13. Log `txHash: ${result.hash}`
14. Log `SMOKE TEST PASSED`
15. Call `wallet.dispose()`
16. Wrap everything in async IIFE with `.catch((e) => { console.error('SMOKE TEST FAILED:', e.message); process.exit(1) })`

CRITICAL anti-patterns to avoid:
- Do NOT log seed phrase, private key, or any key material
- Do NOT use `account.transfer()` with 0 amount (may revert on ERC20)
- Do NOT create WalletManagerEvm without provider param

Add a `"type": "module"` field to `agent/package.json` if not already present (needed for ESM imports in .js files), OR use dynamic import. Check what's compatible with the existing tsx setup. If `agent/package.json` already has `"type": "module"`, the .js file works as ESM. If not, name the file `.mjs` instead.

Since `agent/package.json` does NOT currently have `"type": "module"`, either:
- Add `"type": "module"` to agent/package.json (preferred — tsx supports ESM), OR
- Name the file `wallet-smoke-test.mjs`

Choose the `.mjs` extension to avoid disrupting existing tsx config.

Actually, re-reading agent/tsconfig.json which extends tsconfig.base.json with `module: NodeNext` — this means the workspace already expects ESM-compatible setup. Add `"type": "module"` to `agent/package.json` so both tsx and node work with ESM imports.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && test -f agent/tools/wallet-smoke-test.js && grep -q "SMOKE TEST PASSED" agent/tools/wallet-smoke-test.js && grep -q "sendTransaction" agent/tools/wallet-smoke-test.js && grep -q "dispose" agent/tools/wallet-smoke-test.js && ! grep -q "ESCROW_WALLET_SEED\|seed\|mnemonic\|privateKey\|private_key" agent/tools/wallet-smoke-test.js | grep -i "console.log" && echo "VERIFY PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `agent/tools/wallet-smoke-test.js` exists
    - File contains `WalletManagerEvm` import from `@tetherto/wdk-wallet-evm`
    - File contains `ESCROW_WALLET_SEED` env var check with throw on missing
    - File contains `SEPOLIA_RPC_URL` env var check with throw on missing
    - File contains `getTokenBalance` call for USDT balance check
    - File contains `sendTransaction({ to: address, value: 0n })` for self-send (NOT `transfer()` with 0 amount)
    - File contains string `SMOKE TEST PASSED`
    - File contains `wallet.dispose()` call
    - File contains `SMOKE TEST FAILED` in catch handler with `process.exit(1)`
    - No console.log call contains the words "seed", "mnemonic", "privateKey", or "private_key"
    - `agent/package.json` contains `"type": "module"`
  </acceptance_criteria>
  <done>Smoke test script at agent/tools/wallet-smoke-test.js that generates wallet from seed, checks USDT balance, sends 0 ETH to self, and confirms txHash — runnable with `node agent/tools/wallet-smoke-test.js` from project root</done>
</task>

</tasks>

<verification>
1. `grep -r "new WalletManagerEvm" agent/src/` returns exactly 1 match (singleton)
2. `grep -r "ethers.Wallet" agent/src/` returns 0 matches (WDK only, per WLLT-02)
3. `grep -rn "console.log" agent/src/ agent/tools/` shows no seed/key material logged (WLLT-03)
4. `node agent/tools/wallet-smoke-test.js` (requires .env with real values) prints address, balance, txHash, and "SMOKE TEST PASSED" (WLLT-01)
5. Running smoke test twice with same seed produces same wallet address (deterministic derivation)
</verification>

<success_criteria>
- WDK wallet module exports getWallet/getAccount/dispose from agent/src/wallet/index.ts
- Agent entry point creates wallet on startup and logs address
- Smoke test script exists and is runnable with `node agent/tools/wallet-smoke-test.js`
- Zero instances of raw ethers.Wallet in agent source
- Zero instances of private key logging in agent source
- All 4 WLLT requirements addressed in code
</success_criteria>

<output>
After completion, create `.planning/phases/02-wdk-wallet-escrow-contract/02-01-SUMMARY.md`
</output>
