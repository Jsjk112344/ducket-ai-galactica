---
phase: 08-demo-integration-submission
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified: []
autonomous: false
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03

must_haves:
  truths:
    - "Full demo loop completes end-to-end: scan -> classify -> escrow action"
    - "Mock fallback activates for blocked scrapers — demo never fails"
    - "Dashboard displays live data from agent scan cycle"
    - "All 4 demo segments are observable: agent logic, wallet flow, payment lifecycle, live full loop"
  artifacts: []
  key_links:
    - from: "package.json"
      to: "agent/src/scan-loop.js"
      via: "npm run demo"
      pattern: "scan-loop"
    - from: "agent/src/scan-loop.js"
      to: "agent/src/escrow.js"
      via: "dispatchEscrowAction call"
      pattern: "dispatchEscrowAction"
---

<objective>
Validate the full demo loop end-to-end: run `npm run demo`, verify the agent scan cycle completes (scan -> classify -> escrow), and confirm the dashboard displays live results.

Purpose: Ensure the hackathon demo runs without failure before submission. Three consecutive runs without error is the target.
Output: Human-verified demo readiness
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-demo-integration-submission/08-CONTEXT.md
@.planning/phases/08-demo-integration-submission/08-01-SUMMARY.md
@package.json
@agent/src/scan-loop.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke-test demo script and fix any launch issues</name>
  <files>package.json</files>
  <read_first>package.json, agent/src/scan-loop.js, dashboard/server/api.ts, dashboard/package.json</read_first>
  <action>
1. Run `npm run demo` from the repo root. Watch output for 30 seconds.
2. Verify both processes start:
   - Agent: look for "[agent]" prefix lines showing scan-loop startup (cron scheduled message or immediate first cycle)
   - Dashboard: look for "[dashboard]" prefix lines showing Vite dev server URL (http://localhost:5173) and Express API (port 3001)
3. If either process fails to start, diagnose and fix:
   - Missing dependency: add to correct package.json and run npm install
   - Path error: fix the path in the demo script
   - Port conflict: document workaround
   - Environment variable missing: ensure .env exists (cp .env.example .env if needed)
4. Kill the process after confirming both start successfully.
5. If any code changes were needed, document them.

Note: The agent will likely use mock fallback for scrapers (expected on non-residential IPs). This is acceptable per CONTEXT.md decisions. Look for `source: "mock"` in output.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && node -e "const p=require('./package.json'); if(!p.scripts.demo) { console.error('no demo script'); process.exit(1); } console.log('demo script exists:', p.scripts.demo)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run demo` launches without immediate crash
    - Agent scan-loop process starts (cron message or first cycle output visible)
    - Dashboard Vite dev server starts on port 5173
    - Dashboard Express API starts on port 3001
  </acceptance_criteria>
  <done>Both agent and dashboard launch successfully from `npm run demo`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full demo loop end-to-end</name>
  <files>package.json</files>
  <read_first>package.json, README.md, .env.example</read_first>
  <action>
Present the human-verify checkpoint. The user must run `npm run demo`, observe the full scan-classify-escrow cycle, open http://localhost:5173 in a browser, and confirm all 4 demo segments are observable. No code changes expected — this is a verification-only task.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && node -e "const p=require('./package.json'); if(!p.scripts.demo) process.exit(1); console.log('demo script present')"</automated>
  </verify>
  <what-built>Complete Ducket AI Galactica demo: autonomous fraud detection agent with USDT escrow, React dashboard, and single-command launch via `npm run demo`.</what-built>
  <how-to-verify>
1. From repo root, run: `npm run demo`
2. Wait for the first scan cycle to complete (watch agent output for listing results)
3. Open http://localhost:5173 in browser
4. Verify the 4 demo segments are observable:

   **Segment A — Agent Intelligence:**
   - Agent output shows autonomous scan of StubHub/Viagogo/Facebook (mock or live)
   - Listings appear in agent output with structured JSON fields
   - No human trigger was needed

   **Segment B — Wallet Flow:**
   - Dashboard wallet inspector shows WDK wallet address
   - Shows "client-side only (WDK non-custodial)" indicator
   - Wallet balance is displayed

   **Segment C — Payment Lifecycle:**
   - Agent output shows escrow deposit/release/refund/slash decisions
   - Dashboard escrow status shows active escrows
   - Sepolia txHash links are present (if env vars configured for Sepolia)

   **Segment D — Live Full Loop:**
   - Complete cycle: scan -> classify -> escrow action visible in both agent output and dashboard
   - Dashboard listings table shows color-coded classification badges
   - Clicking a row shows Agent Decision panel with reasoning

5. The full cycle should complete within 5 minutes
6. Kill with Ctrl+C and repeat once more to verify consistency

Type "approved" if demo runs successfully, or describe any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <acceptance_criteria>
    - Full demo loop (scan -> classify -> escrow) completes end-to-end
    - Dashboard shows live listings with classification badges
    - All 4 demo segments (agent, wallet, payment, full loop) are observable
    - Demo completes in under 5 minutes
  </acceptance_criteria>
  <done>Demo validated end-to-end by human. Ready for hackathon submission.</done>
</task>

</tasks>

<verification>
- `npm run demo` starts both agent and dashboard without errors
- Full scan -> classify -> escrow cycle completes
- Dashboard reflects agent data in real time
- Demo completes in under 5 minutes
</verification>

<success_criteria>
- The full demo loop runs end-to-end without failure
- A human has verified all 4 demo segments are observable
- The system is demo-ready for hackathon judges
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-integration-submission/08-02-SUMMARY.md`
</output>
