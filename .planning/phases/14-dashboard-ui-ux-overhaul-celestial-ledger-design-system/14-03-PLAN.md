---
phase: 14-dashboard-ui-ux-overhaul-celestial-ledger-design-system
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - dashboard/src/components/Badge.tsx
  - dashboard/src/components/ConfidenceBar.tsx
  - dashboard/src/components/AgentDecisionPanel.tsx
  - dashboard/src/components/TrustBadges.tsx
  - dashboard/src/components/WalletInspector.tsx
  - dashboard/src/components/ResaleFlowPanel.tsx
  - dashboard/src/components/BuyerLockStep.tsx
  - dashboard/src/components/SettleStep.tsx
  - dashboard/src/components/VerifyStep.tsx
  - dashboard/src/components/ListingForm.tsx
  - dashboard/src/components/EtherscanLink.tsx
autonomous: false
requirements: [UI-08, UI-10]

must_haves:
  truths:
    - "Classification badges use M3 semantic colors (error for scalping, tertiary for legitimate)"
    - "TrustBadges have hover:scale-105 effect and M3 teal color"
    - "AgentDecisionPanel renders with glass panel backdrop-blur effect"
    - "Wallet addresses render in JetBrains Mono (via font-mono class)"
    - "Resale flow stepper uses M3 colors and all 4 steps still work"
  artifacts:
    - path: "dashboard/src/components/Badge.tsx"
      provides: "M3 semantic color map for classification categories"
      contains: "bg-m3-error-container"
    - path: "dashboard/src/components/TrustBadges.tsx"
      provides: "Hover scale micro-interactions"
      contains: "hover:scale-105"
    - path: "dashboard/src/components/AgentDecisionPanel.tsx"
      provides: "Glass panel with backdrop-blur"
      contains: "backdrop-blur"
    - path: "dashboard/src/components/WalletInspector.tsx"
      provides: "M3 surface tokens for wallet display"
      contains: "bg-m3-surface-container"
    - path: "dashboard/src/components/ResaleFlowPanel.tsx"
      provides: "M3 stepper colors"
      contains: "bg-m3-primary-container"
  key_links:
    - from: "dashboard/src/components/Badge.tsx"
      to: "dashboard/src/index.css"
      via: "M3 color tokens"
      pattern: "m3-error-container"
    - from: "dashboard/src/components/AgentDecisionPanel.tsx"
      to: "dashboard/src/components/ConfidenceBar.tsx"
      via: "ConfidenceBar import"
      pattern: "ConfidenceBar"
---

<objective>
Upgrade all detail components to M3 Celestial Ledger styling: Badge semantic colors, ConfidenceBar M3 tokens, AgentDecisionPanel glass panel, TrustBadges hover effects, WalletInspector surface tokens, ResaleFlowPanel stepper, and all resale step components.

Purpose: Complete the visual overhaul — every component uses M3 tokens, creating a cohesive dark theme.
Output: All 11 detail components upgraded to Celestial Ledger design.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-dashboard-ui-ux-overhaul-celestial-ledger-design-system/14-RESEARCH.md

<interfaces>
<!-- Badge.tsx — replace COLOR_MAP values only, preserve BadgeProps and component structure -->
From dashboard/src/components/Badge.tsx:
```typescript
interface BadgeProps { category: string; }
const COLOR_MAP: Record<string, string> = { ... };
// displayText = category.replace(/_/g, ' ')
```

<!-- TrustBadges.tsx — add hover classes to existing span, preserve TRUST_BADGES array -->
From dashboard/src/components/TrustBadges.tsx:
```typescript
const TRUST_BADGES = [
  { label: 'Price cap protected', Icon: Shield },
  { label: 'Verified on-chain', Icon: CheckCircle },
  { label: 'Non-custodial', Icon: Lock },
] as const;
```

<!-- AgentDecisionPanel.tsx — style upgrade only, preserve Classification data destructure -->
From dashboard/src/components/AgentDecisionPanel.tsx:
```typescript
interface AgentDecisionPanelProps { classification: Classification; }
const { category, confidence, reasoning, classificationSource, actionTaken, etherscanLink } = classification;
```

<!-- ResaleFlowPanel.tsx — preserve STEPS array, stepper logic, step content rendering -->
From dashboard/src/components/ResaleFlowPanel.tsx:
```typescript
const STEPS = [
  { id: 1, label: 'List' },
  { id: 2, label: 'Lock' },
  { id: 3, label: 'Verify' },
  { id: 4, label: 'Settle' },
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Badge, ConfidenceBar, AgentDecisionPanel, and TrustBadges</name>
  <files>dashboard/src/components/Badge.tsx, dashboard/src/components/ConfidenceBar.tsx, dashboard/src/components/AgentDecisionPanel.tsx, dashboard/src/components/TrustBadges.tsx</files>
  <read_first>
    - dashboard/src/components/Badge.tsx (current COLOR_MAP — replace values)
    - dashboard/src/components/ConfidenceBar.tsx (current bar colors — swap to M3)
    - dashboard/src/components/AgentDecisionPanel.tsx (current layout — add glass panel)
    - dashboard/src/components/TrustBadges.tsx (current badges — add hover scale)
    - .planning/phases/14-dashboard-ui-ux-overhaul-celestial-ledger-design-system/14-RESEARCH.md (Pattern 3: Glass Panel, Pattern 6: Classification Badge)
  </read_first>
  <action>
**Badge.tsx** — Replace the COLOR_MAP values with M3 semantic colors:
```typescript
const COLOR_MAP: Record<string, string> = {
  SCALPING_VIOLATION: 'bg-m3-error-container text-m3-error border border-m3-error/30',
  LIKELY_SCAM: 'bg-warn-orange/15 text-warn-orange border border-warn-orange/30',
  COUNTERFEIT_RISK: 'bg-warn-yellow/15 text-warn-yellow border border-warn-yellow/30',
  LEGITIMATE: 'bg-m3-tertiary/15 text-m3-tertiary border border-m3-tertiary/30',
  FAIR_VALUE: 'bg-m3-tertiary/15 text-m3-tertiary border border-m3-tertiary/30',
  AT_COST: 'bg-m3-surface-container text-m3-on-surface border border-m3-outline/30',
};
```
Update the fallback from `'bg-gray-500 text-white'` to `'bg-m3-surface-container text-m3-on-surface-variant border border-m3-outline/30'`.

**ConfidenceBar.tsx** — Swap bar colors to M3 tokens:
- Change `'bg-warn-red'` to `'bg-m3-error'`
- Keep `'bg-warn-orange'` (no M3 orange token)
- Change `'bg-success'` to `'bg-m3-tertiary'`
- Change track bg from `bg-bg-card` to `bg-m3-surface-container-highest`
- Change percentage text from `text-gray-400` to `text-m3-outline`

**AgentDecisionPanel.tsx** — Apply glass panel effect and M3 tokens:
1. Change outer div from `bg-bg-card/50 border border-brand-primary/40 rounded-lg p-4` to:
   `backdrop-blur-md bg-m3-surface-container/60 border border-m3-outline/20 rounded-xl p-5`

2. Change all `text-muted-foreground` labels to `text-m3-outline`

3. Change reasoning text from `text-foreground` to `text-m3-on-surface`

4. Change classificationSource text from `text-muted-foreground` to `text-m3-on-surface-variant`

5. Change actionColor mapping:
   - `'text-success'` to `'text-m3-tertiary'` (release)
   - `'text-warn-red'` to `'text-m3-error'` (escrow_deposit)
   - `'text-foreground'` to `'text-m3-on-surface'` (default)

6. Change etherscan link section border from `border-brand-primary/20` to `border-m3-outline/20`

7. Change etherscan link color from `text-brand-accent` to `text-m3-secondary`

CRITICAL: Do NOT change the Classification import, the destructured properties, or the conditional rendering logic.

**TrustBadges.tsx** — Add hover scale and M3 teal colors:

1. Add a 4th trust badge to the TRUST_BADGES array — add `import { Zap } from 'lucide-react'` and:
```typescript
const TRUST_BADGES = [
  { label: 'Escrow Secured', Icon: Shield },
  { label: 'AI Verified', Icon: CheckCircle },
  { label: 'Instant Settle', Icon: Zap },
  { label: 'Non-custodial', Icon: Lock },
] as const;
```

2. Update the span className to:
```
inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full
bg-m3-surface-container text-m3-tertiary border border-m3-tertiary/30
text-xs font-medium cursor-default
hover:scale-105 hover:bg-m3-tertiary/10 transition-transform duration-150
```
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && grep "m3-error-container" dashboard/src/components/Badge.tsx && grep "bg-m3-tertiary" dashboard/src/components/ConfidenceBar.tsx && grep "backdrop-blur" dashboard/src/components/AgentDecisionPanel.tsx && grep "hover:scale-105" dashboard/src/components/TrustBadges.tsx && grep "Escrow Secured" dashboard/src/components/TrustBadges.tsx && cd dashboard && npx vite build --mode development 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/src/components/Badge.tsx COLOR_MAP contains `bg-m3-error-container text-m3-error` for SCALPING_VIOLATION
    - dashboard/src/components/Badge.tsx COLOR_MAP contains `bg-m3-tertiary/15 text-m3-tertiary` for LEGITIMATE
    - dashboard/src/components/Badge.tsx COLOR_MAP contains `AT_COST` and `FAIR_VALUE` entries
    - dashboard/src/components/ConfidenceBar.tsx contains `bg-m3-error` (was bg-warn-red)
    - dashboard/src/components/ConfidenceBar.tsx contains `bg-m3-tertiary` (was bg-success)
    - dashboard/src/components/ConfidenceBar.tsx contains `bg-m3-surface-container-highest` (track bg)
    - dashboard/src/components/AgentDecisionPanel.tsx contains `backdrop-blur-md bg-m3-surface-container/60`
    - dashboard/src/components/AgentDecisionPanel.tsx contains `text-m3-outline` (labels)
    - dashboard/src/components/AgentDecisionPanel.tsx still contains `import { Classification }` from types
    - dashboard/src/components/TrustBadges.tsx contains `hover:scale-105`
    - dashboard/src/components/TrustBadges.tsx contains `Escrow Secured` and `AI Verified` and `Instant Settle` and `Non-custodial`
    - dashboard/src/components/TrustBadges.tsx contains `text-m3-tertiary`
    - `cd dashboard && npx vite build` exits 0
  </acceptance_criteria>
  <done>Badge uses M3 semantic colors, ConfidenceBar uses M3 tokens, AgentDecisionPanel has glass panel effect, TrustBadges has 4 items with hover:scale-105. Vite build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade WalletInspector, ResaleFlowPanel, and all resale step components</name>
  <files>dashboard/src/components/WalletInspector.tsx, dashboard/src/components/ResaleFlowPanel.tsx, dashboard/src/components/BuyerLockStep.tsx, dashboard/src/components/SettleStep.tsx, dashboard/src/components/VerifyStep.tsx, dashboard/src/components/ListingForm.tsx, dashboard/src/components/EtherscanLink.tsx</files>
  <read_first>
    - dashboard/src/components/WalletInspector.tsx (current wallet display — swap bg/text tokens)
    - dashboard/src/components/ResaleFlowPanel.tsx (stepper — swap step indicator colors)
    - dashboard/src/components/BuyerLockStep.tsx (lock step — swap card colors)
    - dashboard/src/components/SettleStep.tsx (settle step — swap outcome colors, preserve OUTCOME_CONFIG)
    - dashboard/src/components/VerifyStep.tsx (verify step — swap panel colors)
    - dashboard/src/components/ListingForm.tsx (form — swap card bg)
    - dashboard/src/components/EtherscanLink.tsx (link — swap color)
  </read_first>
  <action>
**WalletInspector.tsx** — Apply M3 surface tokens:
1. Loading state: change `bg-bg-card` to `bg-m3-surface-container`, `text-muted-foreground` to `text-m3-outline`, `border-brand-primary/20` to `border-m3-outline/20`
2. Error state: change `bg-warn-red/10 border-warn-red/30 text-warn-red` to `bg-m3-error-container/20 border-m3-error/30 text-m3-error`
3. Main container: change `bg-bg-card border-brand-primary/20` to `bg-m3-surface-container border-m3-outline/20`
4. All `text-muted-foreground` labels to `text-m3-outline`
5. All `text-foreground` values to `text-m3-on-surface`
6. All `text-white` values to `text-m3-on-surface`
7. Balance card bg: `bg-bg-primary/50 border-brand-primary/20` to `bg-m3-surface-container-low border-m3-outline/20`
8. Accent colors: `text-brand-accent` to `text-m3-secondary` (ETH/USDT labels, wallet icon)
9. WDK badge: `bg-brand-primary text-brand-accent border-brand-accent/30` to `bg-m3-primary-container text-m3-primary border-m3-primary/30`
10. Network dot: keep `bg-success` as `bg-m3-tertiary`, change `text-success` to `text-m3-tertiary`

**ResaleFlowPanel.tsx** — Update stepper indicator colors:
1. Active step: `bg-brand-primary text-white` to `bg-m3-primary-container text-m3-primary`
2. Completed step: `bg-success/20 text-success` to `bg-m3-tertiary/20 text-m3-tertiary`
3. Upcoming step: `bg-bg-card text-muted-foreground` to `bg-m3-surface-container text-m3-outline`

**BuyerLockStep.tsx** — Read the file first, then apply these substitutions:
- All `bg-bg-card` to `bg-m3-surface-container`
- All `border-brand-primary/20` or similar brand borders to `border-m3-outline/20`
- All `text-muted-foreground` to `text-m3-outline`
- All `text-foreground` to `text-m3-on-surface`
- All `text-white` to `text-m3-on-surface`
- All `text-brand-accent` to `text-m3-secondary`
- All `bg-brand-primary` button backgrounds to `bg-m3-primary-container text-m3-primary`

**VerifyStep.tsx** — Read the file first, then apply:
- Same substitution pattern as BuyerLockStep
- Keep AgentDecisionPanel and Classification imports unchanged

**SettleStep.tsx** — Read the file first, then apply:
- Same substitution pattern as BuyerLockStep
- CRITICAL: Preserve the OUTCOME_CONFIG object and its string.includes() action matching logic
- Update outcome colors: `text-success` to `text-m3-tertiary`, `text-warn-red` to `text-m3-error`

**ListingForm.tsx** — Read the file first, then apply:
- Card/container bg: `bg-bg-card` to `bg-m3-surface-container`
- Borders: `border-brand-primary/*` to `border-m3-outline/20`
- Labels: `text-muted-foreground` to `text-m3-outline`
- Input fields: preserve shadcn Input component usage, just update container styling
- Submit button: update to `bg-m3-primary-container text-m3-primary hover:bg-m3-primary/20`

**EtherscanLink.tsx** — Read the file first, then apply:
- Link color: `text-brand-accent` to `text-m3-secondary`
- Any other brand color references to M3 equivalents

CRITICAL for ALL files: Do NOT change any logic, props, state management, API calls, or conditional rendering. These are CSS class swaps only.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica && grep "bg-m3-surface-container" dashboard/src/components/WalletInspector.tsx && grep "bg-m3-primary-container" dashboard/src/components/ResaleFlowPanel.tsx && grep "text-m3-outline" dashboard/src/components/WalletInspector.tsx && grep "OUTCOME_CONFIG" dashboard/src/components/SettleStep.tsx && cd dashboard && npx vite build --mode development 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/src/components/WalletInspector.tsx contains `bg-m3-surface-container` (main container)
    - dashboard/src/components/WalletInspector.tsx contains `text-m3-outline` (labels)
    - dashboard/src/components/WalletInspector.tsx contains `text-m3-secondary` (accent colors)
    - dashboard/src/components/WalletInspector.tsx contains `bg-m3-primary-container` (WDK badge)
    - dashboard/src/components/WalletInspector.tsx does NOT contain `bg-bg-card` (fully migrated)
    - dashboard/src/components/WalletInspector.tsx does NOT contain `text-brand-accent` (fully migrated)
    - dashboard/src/components/ResaleFlowPanel.tsx contains `bg-m3-primary-container text-m3-primary` (active step)
    - dashboard/src/components/ResaleFlowPanel.tsx contains `bg-m3-tertiary/20 text-m3-tertiary` (completed step)
    - dashboard/src/components/ResaleFlowPanel.tsx contains `bg-m3-surface-container text-m3-outline` (upcoming step)
    - dashboard/src/components/SettleStep.tsx still contains `OUTCOME_CONFIG`
    - dashboard/src/components/SettleStep.tsx still contains `.includes(` (action matching logic preserved)
    - dashboard/src/components/BuyerLockStep.tsx does NOT contain `bg-bg-card` (fully migrated)
    - dashboard/src/components/ListingForm.tsx contains `bg-m3-surface-container`
    - dashboard/src/components/EtherscanLink.tsx contains `text-m3-secondary`
    - `cd dashboard && npx vite build` exits 0
  </acceptance_criteria>
  <done>All 7 remaining components migrated to M3 tokens. WalletInspector shows M3 surface containers with JetBrains Mono for addresses. ResaleFlowPanel stepper uses M3 purple/teal/muted. All resale step components (BuyerLock, Verify, Settle, ListingForm) use M3 surfaces. EtherscanLink uses M3 secondary gold. All logic preserved. Vite build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Celestial Ledger design</name>
  <what-built>Complete Celestial Ledger design system applied to all 14 dashboard components. Fixed nav, hero with Logo_2.png, 4 stat cards with border-l-4 + progress bars, Active Order Book table with chevron rows, glass panel AgentDecisionPanel, M3 classification badges, hover-scale trust badges, FAB button, JetBrains Mono for wallet addresses, and M3 token migration on all resale flow components.</what-built>
  <how-to-verify>
    1. Start dev server: `cd dashboard && npm run dev`
    2. Open http://localhost:5173 in browser
    3. Verify fixed nav stays visible when scrolling — shows DUCKET AI logo, nav links, v2.0 badge
    4. Verify hero section shows Logo_2.png, "Celestial Ledger" title, "Protocol v2.0" badge, green pulse node indicator
    5. Verify FAB (gold circle with + icon) is visible bottom-right on all tabs
    6. Click "Listings" tab — verify "Active Order Book" header, chevron icons on rows, first seed row auto-expanded
    7. Verify classification badges use new colors (red container for SCALPING_VIOLATION, teal for LEGITIMATE)
    8. Verify AgentDecisionPanel expanded panel has glass/blur effect
    9. Hover trust badges — verify scale-up animation
    10. Click "Escrow" tab — verify 4 stat cards have colored left borders and progress bars
    11. Click "Wallet" tab — verify addresses render in JetBrains Mono, purple surfaces, gold USDT label
    12. Click "Resale Flow" tab — verify stepper colors (purple active, teal completed, muted upcoming), walk through all 4 steps
    13. Check browser DevTools console for any errors
  </how-to-verify>
  <files>none</files>
  <action>Human visual verification of Celestial Ledger design. Start dev server and verify all components match mockup.</action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-ai-galactica/dashboard && npx vite build</automated>
  </verify>
  <done>User approves visual appearance of all dashboard tabs</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd dashboard && npx vite build` exits 0
- No component contains `bg-bg-card` (fully migrated from old brand tokens to M3)
- `grep -r "hover:scale" dashboard/src/components/TrustBadges.tsx` matches
- `grep -r "backdrop-blur" dashboard/src/components/AgentDecisionPanel.tsx` matches
- `grep -r "OUTCOME_CONFIG" dashboard/src/components/SettleStep.tsx` matches (logic preserved)
</verification>

<success_criteria>
Every dashboard component uses M3 Celestial Ledger tokens. No old brand-only tokens (bg-bg-card, text-brand-accent) remain as primary colors — M3 equivalents used everywhere. All interactive effects work (hover scale, chevron rotate, glass blur, pulse indicator). All existing resale flow logic, data display, and tab navigation preserved. Visual match to Celestial Ledger mockup confirmed by user. Vite build green.
</success_criteria>

<output>
After completion, create `.planning/phases/14-dashboard-ui-ux-overhaul-celestial-ledger-design-system/14-03-SUMMARY.md`
</output>
